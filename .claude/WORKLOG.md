# WORK LOG — what actually got done

Organised by **session agenda**. Adarsh gives an agenda at the start of each
chat; the agent writes that agenda here immediately, then ticks items off as
they complete. Purpose: at month end, one scroll tells you how you got here.

Terse by design. Detail belongs in `docs/`, not here.

---

## How the agent maintains this

**At chat start**, as soon as the agenda is given — before any work — append a
session block under the current month:

```
### DD · <session title, 3–6 words>
**Agenda:** 1) <item> 2) <item> 3) <item>
```

**As each agenda item finishes**, append one line under that block:

```
- ✅ <item> — <what changed, plain language> · `file`, `file` · <state>
```

- `<state>`: `local` · `pushed` · `wip` · `verified` · `reverted` · `blocked: <why>`
- Status marks: `✅` done · `⏸` blocked/deferred · `❌` dropped (say why) ·
  `➕` unplanned work that came up mid-session
- Prefix `⚠` if something was found broken and left unfixed.
- **Never rewrite past entries.** Corrections are new lines.
- New month → new `##` heading, newest month at the top.
- If a session ends with agenda items untouched, mark them `⏸ carried to next
  session`.

---

## 2026-08

### 10 · Architecture kickoff
**Agenda:** 1) Explain the whole system end-to-end in plain language (backend, architecture, functioning, data flow) 2) Cover both perspectives — us building it, and photographers/guests using it 3) Lay out alternatives at each layer 4) Cost model for the zero-revenue starting phase 5) Agree the build order and the first decisions

- ✅ Repo scaffolding for the concurrency protocol — created `.claude/ACTIVE-WORK.md` (fresh, empty claim tables) and this `WORKLOG.md`. Repo is otherwise **empty: git init'd on `main`, zero commits, zero files.** · `.claude/*` · `wip`
- ✅ 1–4 Full architecture walkthrough delivered in chat — five-box model (Ingest → Brain → Store → Serve → Money), per-layer tech choice + alternative + price, per-wedding cost model in ₹, and a trace of one photo and one guest search through the system. Nothing built yet. · `—` · `verified`
- ⚠ Two commercial blockers surfaced, unresolved, **must be checked before any paid customer**: (a) InsightFace **pretrained model weights are non-commercial-research licensed** — the code is fine, the weights are the problem; (b) Google Drive `drive.readonly` is a **restricted scope** requiring a paid annual CASA security assessment — the dodge is Google Picker + `drive.file` scope, or browser upload for v1. Also noted: Vercel Hobby tier forbids commercial use.
- ⏸ 5 Build order proposed; awaiting Adarsh's answers on v1 ingestion route, who the paying customer is, and where the worker runs. No code written this session by design — the ask was discussion.
- ✅ 5 Decisions locked: **ingestion = browser folder upload** (Drive deferred, which also defers the restricted-scope problem); **worker = own desktop**, written as a queue puller so a rented GPU is a config change later; **no GPU and no real wedding photo sets yet** → CPU-only, and sourcing test data is now the top non-code task. · `—` · `verified`
- ➕ Review document built — `docs/architecture.html`, single self-contained file (no JS, no external assets, light/dark aware) covering all of the above in 12 numbered sections, ending with 5 numbered open questions for Devesh. Verified in-browser: all anchors resolve, no horizontal overflow at 375px or desktop. **Not committed — Adarsh reviews and pushes.** · `docs/architecture.html` · `verified`
- ➕ Consequence of no-GPU/no-data: pipeline must be **split into a cached expensive stage (detect+embed, written to disk once) and a cheap tunable stage (clustering, re-runs in seconds)**. Without this, CPU-only iteration on clustering thresholds is unworkable. Locked as an architectural requirement before any code.

### 10 · Answers to open questions + test dataset script
**Agenda:** 1) Record Devesh's answers to Q1–Q5 in `docs/architecture.html` 2) Build a script to assemble a test photo dataset 3) Commit the changes

- ✅ Q1 Hardware — answered: keep it API-based, nobody runs anything on their own machine; stays on local hardware for now per §9, reach for GPU-on-demand (rented, per-job) rather than owning one if/when local isn't enough. · `docs/architecture.html` · `local`
- ✅ Q2 Test photos — answered: bootstrap with cricket players (checked for an existing open dataset that fit better first, found none) — public figures, many photos each across different events/angles/lighting, closer to the wedding shape than studio headshots. Wrote `scripts/build_test_dataset.py`, stdlib-only, pulls freely-licensed images + a license/provenance manifest from Wikimedia Commons for a configurable player list. Test run (2 players, 3 images each) verified real JPEGs download with correct metadata; hit a 429 mid-run so added retry-with-backoff. Explicitly **not** a substitute for real wedding photos when tuning clustering thresholds (§11) — only proves detect→embed→cluster runs end to end. · `docs/architecture.html`, `scripts/build_test_dataset.py` · `verified`
- ✅ Q3 Who pays — answered: no single answer, depends on channel — photographer pays if we sell direct to them, customer (couple/family) pays if we sell to them. Both stay open. · `docs/architecture.html` · `local`
- ✅ Q4 Anything different — answered: agreed with §4 as written (R2/Postgres), no changes; confirmed §10's pipeline-before-UI order — validate clustering works before spending any time on UI. · `docs/architecture.html` · `local`
- ⏸ Q5 Time — not answered this session, carried forward.
- ➕ Added `.gitignore` (`test-data/`, `__pycache__/`, `*.pyc`) so generated/downloaded dataset images never get committed as binary blobs. · `.gitignore` · `local`

### 11 · Resolution & storage architecture review
**Agenda:** 1) Stress-test Adarsh's suspicion that computing on compressed previews instead of originals costs accuracy 2) Evaluate a ~100 GB Google Drive (or equivalent) staging arrangement — all alternatives and outcomes 3) Work out how compute runs against that storage while it stays on local hardware, and what changes when it moves to rented GPU 4) Freeze the architecture

- ✅ 1 Adarsh's suspicion **confirmed and was a real bug** — Draft 1 shrank to 1000px *before* detecting, which drops a 90px back-row face to 15px. Worse than missed photos: undersized crops get upsampled into ArcFace, embeddings collapse toward a "generic face" region, and the system returns **confident wrong matches** — the exact failure §4 said would kill trust. Diagnosis corrected though: it's a **pipeline-ordering** problem, not a storage one. Detect+embed at native res in memory, shrink only for delivery, discard original. Cost ₹0. · `—` · `verified`
- ✅ 1 Added three things Draft 1 was missing without which "process the original" achieves nothing: **tiled detection** (SCRFD shrinks its input to 640 internally, so a full-res photo alone doesn't help), **face alignment** via 5-point landmarks, and **EXIF rotation** applied first. · `—` · `verified`
- ✅ 2 100 GB staging arrangement **rejected as unnecessary** — the accuracy fix needs zero persistent storage (photos pass through, they don't park). Also: Google Drive is the wrong tool at the same price as R2 (~₹130–210/mo) with worse API + rate limits; the quality-loss worry was about Google *Photos*, not Drive, which stores bit-exact. For the tuning phase the answer is an external drive, ₹0/month. · `—` · `verified`
- ✅ 3 Compute plan: desktop + Cloudflare Tunnel for the pilot (free public HTTPS to a home machine, no cold starts, sidesteps every free tier that sleeps or bans commercial use); rented GPU at ~₹20–30/wedding once earning. Hard rule added — **worker never references local filesystem paths, object URIs only** — or the GPU move becomes a rewrite. · `—` · `verified`
- ✅ 4 Architecture frozen and written up as **`docs/ARCHITECTURE.md`** — full 8-step flow (Ingest → Manifest → Process → Cluster → Publish → Review → Gallery → Search), each step with tech choices, edge cases and a free-now/paid-later column; plain-language pixel primer; complete SQL data model; landmines; build order. Supersedes `docs/architecture.html` (kept for history, not deleted). · `docs/ARCHITECTURE.md` · `local`
- ➕ **v1 ingestion changed from browser upload to link-first** (Adarsh confirmed 100%) — reverses a 10 Aug locked decision. Photographers already deliver by link; re-uploading 50 GB through a tab held open for 3h is a new, worse ask. Adapter pattern over Drive/Dropbox/direct-zip/OneDrive/browser-upload so the pipeline never knows the source. · `docs/ARCHITECTURE.md` · `local`
- ➕ **Google Drive restricted-scope landmine defused** — the CASA audit only applies to `drive.readonly` on a user's *account*. A public "anyone with the link" folder is readable anonymously with our own API key: no OAuth, no restricted scope, no annual audit. **⚠ Unverified — needs proving against a real public folder before we build on it.** Biggest single finding of the session. · `docs/ARCHITECTURE.md` · `blocked: needs a real Drive link to verify`
- ➕ **Download policy locked: A+B** — 2048px preview always, plus "download full size" that proxies the original live from the photographer's source via the saved `source_file_id`, falling back to the preview if the source is gone. True originals at **₹0 storage**. Only possible because of link-first ingest; also closes a hole nobody had caught — Draft 1 promised Drive-linked original downloads while locking browser upload, under which no Drive pointer exists at all. · `docs/ARCHITECTURE.md` · `local`
- ➕ **Supabase free tier would have died at wedding #2**, not 4–5 as Draft 1 claimed (~200 MB/wedding of `vector(512)` rows + index against a 500 MB cap). Fixed by splitting storage: ~147 person-centroids in Postgres (~300 KB), 32k individual fingerprints in `faces.parquet` on R2 (33 MB float16) → ~5 MB/wedding, 40+ weddings free. Search becomes two-tier: centroids for the 90% case (~5 ms), brute-force numpy over the parquet as fallback (~10 ms for 32k vectors). **A dedicated vector DB is solving a problem we don't have at this scale.** · `docs/ARCHITECTURE.md` · `local`
- ➕ Clustering designed properly: **agglomerative average-link** (one tunable knob, deterministic — repeatability matters while tuning) over k-means (needs the answer up front) and DBSCAN (assumes uniform density; bride has 2,104 faces, a guest has 4). **Two-pass** — tight first into ~380 pure fragments, then merge with evidence, including the free **same-photo rule: two faces in one photo can never be the same person**. Safety rule: splitting is a mild failure, merging is a catastrophe, always err tight. Noise faces kept with `person_id = NULL`, not discarded — Tier-2 search is what finds the cousin who's in 3 photos. · `docs/ARCHITECTURE.md` · `local`
- ➕ **`embedding_model_version` stamp added to `events`/`faces`/`persons`** — without it, a future model swap makes old and new fingerprints mutually unreadable with no error: guests silently stop finding photos forever. Free now, means reprocessing every wedding if retrofitted. · `docs/ARCHITECTURE.md` · `local`
- ➕ Materialised **`person_photos`** join table so display is a single indexed read (~5 ms) with zero vector maths at request time — all the thinking happens once, overnight. Sub-album captured from source folder names (`Sangeet/`), giving "Priya's Sangeet photos" as a free feature. · `docs/ARCHITECTURE.md` · `local`
- ⚠ **The cricket-player test set cannot validate any of the resolution work** — cropped press photos, big faces filling the frame. Proves detect→embed→cluster runs; cannot test the back-row-of-a-group-photo case, which is now the main technical risk. Two real files needed: one public Drive wedding folder link, one 24 MP group shot. Both open, Adarsh sourcing.

### 12 · Initial development — Stage 1 & Stage 2
**Agenda (inferred from "Lets start with initial development", confirmed against `docs/ARCHITECTURE.md` build order Week 1–2):** 1) Repo scaffolding — package layout, deps, the object-URI storage interface that keeps the GPU migration cheap 2) Ingest adapter interface + first implementations 3) Stage 1: per-photo pipeline (EXIF → tiled native-res detect → align → quality gate → ArcFace embed → derivatives) writing `faces.parquet` + face crops 4) Stage 2: two-pass clustering with the same-photo rule 5) The HTML accuracy meter we stare at for two weeks 6) Prove it end to end on real image files

- ✅ 1 Repo scaffolding — `wff/` package created (config, storage, ingest, process, cluster, stage1, stage2, report, cli) + `requirements.txt` + `.gitignore` for `.wff-store/`. **Deliberately did NOT depend on the `insightface` package**: no prebuilt wheel for CPython 3.12 on Windows (needs MSVC), its `FaceAnalysis` wrapper won't expose per-tile detection, and keeping SCRFD/ArcFace behind our own `Detector`/`Embedder` interfaces makes landmine #1 (non-commercial weights) a one-file swap. We load the ONNX weights directly and implement anchor decoding + Umeyama alignment ourselves. · `wff/**`, `requirements.txt`, `.gitignore` · `local`
- ✅ 1 **The no-local-paths rule is enforced, not documented** — `parse_uri()` raises on anything without a scheme, so `D:\photos\x.jpg` or `events/ev1/x.jpg` is rejected at the boundary. Only `LocalObjectStore` and the dev-only `LocalFolderAdapter` know a filesystem exists. Test `test_local_paths_are_rejected` locks it. R2 backend deliberately unwritten (decided: local for week 1) — it is a ~60-line boto3 subclass with zero pipeline changes. · `wff/storage/**`, `tests/test_storage.py` · `verified`
- ✅ 2 Ingest adapters — `IngestAdapter` interface + `FileRef` (carries `source_id` for download policy B, and `sub_album` free from folder names). Implemented `LocalFolderAdapter` and `GoogleDriveFolderAdapter` (public folder, anonymous, our own API key, paginated + iterative walk + exponential backoff on 403/429). Dropbox/zip/OneDrive designed but not written — one file each, nothing downstream changes. Added `wff verify-drive <link>` to settle **open item #1** in one command; still **UNVERIFIED**, no real public folder to test against. · `wff/ingest/**` · `local`
- ✅ 3 Stage 1 built and **running end to end on real photos** — 17 images → 29 faces detected → 23 accepted → `faces.parquet` + previews + thumbs + aligned crops, at **0.69 s/photo** CPU-only. Order is the Draft-1 correction: EXIF rotate → detect at native res (whole image + tiles) → align → quality gate → embed → *only then* shrink. · `wff/process/**`, `wff/stage1.py` · `verified`
- ✅ 3 **Tiling measured, not assumed** — built a synthetic 6000×4000 group photo from real faces at controlled heights and ran detection with tiling off vs on. Result: 400px/200px faces — no gain (whole-image pass is fine); **120px → 10/12 vs 12/12; 90px (back row of a group) → 9/12 vs 12/12; 60px ("dancing across the room") → 0/12 vs 11/12.** At 60px the single whole-image pass finds **nothing at all**. This is the first empirical support for the central claim of Draft 2. Caveat: composited real faces on a synthetic canvas, so it isolates *scale* only — real back-row faces also carry motion blur and focus falloff, so treat these as an upper bound. · `verified`
- ✅ 3 Anti-aliasing in alignment — a 1500px bride close-up mapped to 112px is a 13× reduction, and `warpAffine`+`INTER_LINEAR` point-samples it into an aliased mess that moves the embedding. Large reductions now pre-shrink an ROI with `INTER_AREA` and refit. Locked by a test that warps a 1px-stripe checkerboard and asserts it goes flat. · `wff/process/align.py` · `verified`
- ✅ 3 Resumability — three append-only journals; **a photo counts as done only when its line lands in `state.jsonl`, written last**. Torn embedding rows are padded (never overlapped) so offsets already recorded stay valid; faces from an attempt that died before committing are dropped at consolidation. Verified by resuming a real run (17/17 skipped). · `wff/stage1.py`, `tests/test_stage1_journal.py` · `verified`
- ✅ 4 Stage 2 two-pass clustering — tight pass 1, then evidence-based merge with the **same-photo rule**, checked against *merged* photo sets so a merge chain (A+B, then B+C where A and C co-occur) cannot defeat it. Second-class 50–80px faces may join a person but never start one. Leftovers keep `person_id = NULL` and are never deleted. · `wff/cluster/**` · `verified`
- ✅ 4 ➕ **Found a hole the architecture doc does not address: exact agglomerative clustering is O(n²) memory** — ~1.1 GB at 12,000 faces, ~8 GB at the 32,000 a real 4,000-photo wedding produces. It would have died at wedding #1 with an OOM, not a wrong answer. Fix: above 12k faces, pre-partition with k-means and run exact agglomerative inside each block. Blocking can only *split*, never merge — the safe direction — and pass 2 recovers the splits. Fixed seed, so runs stay repeatable. · `wff/cluster/two_pass.py` · `local`
- ✅ 5 The accuracy meter — `report.html` per event: headline counts, people grid of aligned crops with **each face's height in the ORIGINAL photo printed on it**, second-class faces outlined orange, contaminated clusters flagged red and sorted to the top, the leftovers section, what the gate rejected and why, and every threshold used printed with its env var so a run is reproducible. Verified rendering in-browser. · `wff/report.py` · `verified`
- ✅ 5 ➕ Free ground-truth scoring — the cricket set puts one person per folder, so the folder *is* the answer. Stage 2 auto-detects that layout and reports pairwise precision/recall/F1, contaminated clusters (merges — the catastrophe) and split counts (the nuisance), asymmetrically, per the safety rule. Real weddings have no labels and it silently skips. · `wff/stage2.py`, `wff/cluster/metrics.py` · `verified`
- ✅ 6 Proven end to end: `wff run ev_cricket01 test-data/cricket-players` → **precision 1.000, recall 0.075, 0 contaminated clusters**. Precision is the number that matters and nothing was wrongly merged; recall is meaningless here because the dataset has 1–2 faces for 8 of 9 people, so almost everything is a leftover by design (`min_faces_per_person=3`). **This validates the machinery, not the accuracy.** · `verified`
- ✅ 6 ➕ 52 tests, all passing — concentrated on what fails *silently*: tile grid coverage/overlap/target-scale, NMS, truncated-face rejection at interior tile edges, Umeyama recovering a known transform, landmarks landing on the ArcFace template, anti-aliasing, the same-photo rule (incl. merge chains), second-class rules, leftover retention, repeatability, centroid unit-length, journal crash/resume, and the model-version filter. · `tests/**` · `verified`
- ⚠ ➕ **Parquet bug found and fixed**: pyarrow writes a null inside a `fixed_size_list` happily and then **cannot read it back** (`Expected all lists to be of size=512 but index N had size=0`). Stage 1 died at first consolidation. Rejected faces now carry an empty list in a variable-length column; accepted faces all carry exactly 512 so stacking is still dense. Regression test added. · `wff/stage1.py` · `verified`
- ⚠ ➕ **Test dataset is still not usable and this is now the blocker for any tuning.** Wikimedia Commons rate-limits (HTTP 429) full-size downloads hard enough that a 140-image run produced **18 images, 1–2 per person**. Patched `build_test_dataset.py` to request rendered thumbnails (`iiurlwidth`), which Commons' own 429 body recommends — kept 5/5 per player in a spot check vs ~1/14 before. A full rebuild still failed because a **pre-patch background run I could not terminate kept consuming the shared rate budget**. Retry when that process is gone. Note this set could never test the resolution work anyway (§cropped press photos). · `scripts/build_test_dataset.py` · `blocked: Wikimedia throttling + stale process`
- ⏸ Threshold tuning (pass-1 0.42 / pass-2 0.55 are **untuned guesses**, not measurements) — carried. Needs either a rebuilt cricket set or, far better, real photos. **Open items #1 (a public Drive folder link) and #2 (one 24 MP group shot) remain the highest-value things available and are still unowned by code.**
- ✅ ➕ **Embedder validated on real faces, and the first honest calibration data point.** Four faces *visually confirmed* to be the same person, spanning **60px to 695px** in their original photos, sit at pairwise cosine distance **min 0.287 / median 0.347 / max 0.451**; cleanly-different people sit at **≥0.849**. Clear separation, so ArcFace preprocessing (BGR→RGB swap, (x−127.5)/127.5, 112×112 aligned crop) is correct. Note the worst same-person pair (0.451) is **above** `pass1_threshold=0.42` — pass 1 alone would have split them, and average-link agglomerative is what pulled them together. So 0.42 sits just on the tight side of the real boundary, which is the intended "err tight" design, but it is now a *measured* position rather than a guess. · `verified`
- ⚠ ➕ **The folder-name ground truth is unreliable on this dataset, and it is not just a resolution problem.** Press photos contain crowds: one 10-photo run detected 49 faces, and a photo in `virat-kohli/` contains other cricketers at ≥110px who all inherit the label `virat-kohli`. That pollutes "same person" pairs so badly that same-person median (0.917) looked indistinguishable from different-person median (0.962) — which would read as a broken embedder if taken at face value. `docs/ARCHITECTURE.md` warns this set cannot test the resolution work; it also cannot be trusted for *recall* or threshold tuning. **Precision remains meaningful** (0 wrong merges out of 75 different-person pairs at both 0.42 and 0.55). Any future auto-scoring needs one-face-per-photo data or hand-labelled faces.

### 12 · Orientation — what the thing actually is
**Agenda (inferred from Adarsh's first message, no agenda given):** 1) Explain in plain language what is being built, what form it takes today, and how he sees it with his own eyes 2) Confirm whether he can point it at his own photo folders locally, and how 3) Write into `CLAUDE.md` that he is building software for the first time, so every agent uses layman's language

- ✅ 3 Created project `CLAUDE.md` with **"talk to Adarsh in plain language" as rule #1** — his words: jargon-heavy explanations and questions "go directly above me" and we both end up confused burning tokens. Includes worked bad-vs-good examples (ask about the product consequence, never the plumbing; if a decision has no consequence he could feel, decide it silently and mention it in one line), plus the day-one context an agent needs: one-paragraph product, CLI-only state, the run command, the four standing code rules, the real bottleneck. · `CLAUDE.md` · `local`
- ✅ 1,2 Explained in chat: today it is a **terminal command, not an app** — `python -m wff run <event-id> "<folder>"` on any folder on this machine, output is `report.html` opened in a browser. Website is Week 3–4 by design. Confirmed local folders work: `LocalFolderAdapter` accepts a bare Windows path, walks subfolders, nothing uploads. No code changed.

### 12 · Real photos — first run on an actual folder
**Agenda:** 1) Finish the initial-development task 2) Run the pipeline on Adarsh's real photo folder (`D:\Drive Backup - Manali, kedarnath\...\Photos from 2023`)

- ✅ 1 Dataset script bug found and fixed — `download()` had 429 retry-backoff but **`api_get()` had none**, so a throttled API call raised straight out and killed every player after it (a 12-player run died at player 5). Now retries with backoff, honours `Retry-After`, and per-player failures are caught. Verified live: hit a 429, waited 36s, recovered, completed. Commons still throttles this IP so yield stays low — moot now that real photos exist. · `scripts/build_test_dataset.py` · `verified`
- ✅ 2 **Stage 1 run on 851 real photos, zero failures.** 7.0 GB Google Takeout: 641 iPhone `.HEIC` + 178 JPG decoded correctly (12.2 MP, EXIF timestamps read), with 231 videos and 1,254 JSON sidecars correctly skipped. **1,806 faces detected, 1,182 accepted (65.4%), 2.27 s/photo** CPU-only. 704 of 851 photos contain at least one face. · `verified`
- ✅ 2 **The face-size numbers are now measured, not assumed.** Every figure in `docs/ARCHITECTURE.md` was arithmetic on a hypothetical camera. Real distribution across 1,806 detections: 22.1% over 400px, 36.8% 200–400px, 12.2% 112–200px, 6.5% 80–112px, 10.9% 50–80px, 11.5% under 50px. **28.9% of all detected faces are below 112px — the size ArcFace actually needs.** That is the product's whole risk surface, quantified for the first time. · `verified`
- ✅ 2 Quality gate on real data: rejected 236 turned away, 207 too small, 123 low confidence, 58 too blurry. 80 of the accepted faces are second-class (50–80px). · `verified`
- ✅ 2 **Clustering works on real photos.** 1,182 faces → 159 tight pass-1 groups → 66 evidence-based merges → **20 people** + 155 leftovers. Eyeballed the report: Person 1 = 241 faces across 241 photos, every crop visibly the same man across sunglasses/lighting/angle changes; Persons 3, 5, 6, 7 likewise clean. **The same-photo rule fired on real data and blocked 1 merge.** · `verified`
- ✅ 2 **Cluster quality measured.** All 20 clusters tight — within-cluster p95 distance from centroid 0.02–0.46, none flagged loose. Critically, the **closest pair of different people sits at 0.611 while `pass2_threshold` is 0.55** — only 0.061 of margin. So 0.55 is appropriately conservative and **~0.61 is the empirical ceiling** on this data; raising it further would start merging real people. First real calibration of that knob. · `verified`
- ⚠ ➕ **Real bug found by the real data: a duplicate detection slipped through NMS.** Person 7 came out with *48 faces in 47 photos* — impossible. Cause (IMG_3526.HEIC): the whole-image pass found a face at 286×389 and a tile found a degenerate 116×382 **sliver of the same face**; their IoU was **0.398**, a hair under the 0.40 threshold, so both survived and both got embedded. IoU systematically under-penalises a small box sitting inside a large one because the union is inflated. Fixed by suppressing on **containment** (intersection ÷ smaller box's area > 0.80) as well as IoU, plus a degenerate-shape gate — justified by measuring 1,806 real boxes: median w/h 0.787, p1 of accepted 0.627, only 2 below 0.35 and both were slivers. Verified on the offending photo: 5 detections → 4, duplicate gone, the 2 accepted faces are 2 genuinely different people. Two regression tests added, incl. one asserting a small nearby face (a child in front of an adult) is **not** suppressed. · `wff/process/detect.py`, `wff/process/quality.py`, `wff/config.py` · `verified`
- ⚠ The `ev_manali01` numbers above were produced **before** the NMS fix, so they contain ~1 duplicate face out of 1,182. Not re-run — the effect is far below anything that changes a conclusion.
- ✅ 2 **Tiling A/B on real photos — the honest number, and it is far smaller than the synthetic test suggested.** Ran all 851 photos twice, tiling on vs off. Tiled: 1,806 detected / 1,172 accepted at 2.27 s/photo. Plain: 1,433 detected / 1,128 accepted at 0.97 s/photo. So tiling buys **+44 accepted faces (+3.9%)** and costs **2.34× CPU** — which does confirm the doc's "~2× CPU time" estimate. Where the gain lands is exactly as predicted: **faces under 112px went 85 → 126 (+48%)**. · `verified`
- ⚠ **Why the gain is only +3.9% here: this folder is not a wedding.** 347 of 704 face-bearing photos contain exactly ONE face and the maximum is 24; it is a trip folder of selfies and small groups, so only 10.9% of faces fall in the 50–80px band. The back-row-of-a-30-person-group case that tiling exists to solve is essentially absent from this data. The synthetic test (0/12 → 11/12 at 60px) and this test (+3.9%) are **not in conflict** — they measure the same mechanism against different subject matter. **Tiling's value is a function of how many group shots a set contains, and a real wedding folder is still needed to price it properly.** Worth considering an adaptive rule later (tile only when the whole-image pass suggests a crowded frame) rather than paying 2.3× on selfie-heavy sets.
- ⚠ ➕ **Second NMS weakness found, same family as the sliver bug.** On 10 photos tiling accepted FEWER faces (net −11). Cause: a tile detection **outscored** the whole-image detection of the same face and won NMS, then failed the quality gate — 12 "turned away", 7 "too small", 2 "low confidence", and every one of those rejected faces came from a tile pass. NMS keeps the highest-*scoring* box, but a tile's view of a face can be geometrically worse (tighter/partial box, different landmarks → different yaw) while still scoring higher. The containment fix handles the extreme case; a fuller fix is to prefer the larger box when scores are close, or to defer the choice until after the quality gate. **Not implemented — flagging it as a known limitation rather than tuning blind against a non-wedding folder.**

### 12 · Testing platform — a UI Adarsh & Devesh can both drive
**Agenda:** 1) Install the `ui-ux-pro-max` skill from GitHub 2) Build a simple local web UI: paste a folder path/link, it runs the whole pipeline across every image inside 3) Show the report in the browser — every detail, all clusters exhaustively, nothing truncated, for manual checking 4) Let a reviewer leave feedback in place so the model can improve 5) Light theme

- ✅ 1 Skill was **already installed** — `ui-ux-pro-max@ui-ux-pro-max-skill` v2.11.0 from that exact repo, in the plugin cache since 22 Jul. Used it for the design: took its palette (blue data + amber highlight on `#f8fafc`) and Fira Sans/Fira Code pairing; **rejected** its suggested "Exaggerated Minimalism" style and "Real-Time Operations Landing" pattern — `clamp(3rem,10vw,12rem)` headings and massive whitespace are a landing-page look, and this screen exists to fit 4,000 face crops on it. Body text is slate not its blue `#1E3A8A`: that passes contrast but an all-blue wall of numbers is unreadable. · `wff/web/static/app.css` · `verified`
- ✅ 2,5 `python -m wff ui` → light-theme page on localhost:8765. Paste a folder, press Start, watch a live progress bar with a working **Stop**. Runs the same `build_manifest`/`process_event`/`consolidate`/`run_stage2` the CLI runs — **no second pipeline**, or the console's numbers would be worth nothing. Verified end to end through the browser: 24 photos → 14s → auto-reload into results. Jobs are serialised on one worker thread (Stage 1 already uses every core). Progress is read from `state.jsonl` rather than instrumented into Stage 1 — that journal is already the authority on what's done, so the bar stays correct across a resume. · `wff/web/jobs.py`, `wff/cli.py` · `verified`
- ✅ 3 **Every pile in full, nothing truncated.** Person 1 of `ev_manali01` renders all **241** crops; the old `report.html` capped at 60, which hid exactly where mistakes live. Each crop carries its height in the original photo; click it and the **original photo opens with every detected face boxed** — positioned as percentages of the original, so it survives any future preview-size change. Full page 686 KB, lazy images, no horizontal scroll at 375px. · `wff/web/templates/event.html` · `verified`
- ✅ 3 ➕ **Threshold knobs are query params, so a tuning run is a shareable URL.** Re-group takes **0.26s** against the cached parquet (5.25s on first load) — the expensive/cheap stage split finally paying off as a UI. Silly values are clamped, not crashed. · `wff/web/data.py` · `verified`
- ✅ 4 **Feedback is stored as claims about FACE IDS, never `person_id`.** This is the decision the whole feature rests on: person ids are reshuffled by every threshold change, so a judgement keyed to "Person 3" is worthless one re-group later. "This pile is one person" becomes a `same_group` of its face ids; "this face isn't them" becomes `different_pairs`. Union-find over those claims produces partial ground truth that `cluster/metrics.py` can already score. **Consequence: precision/recall on a real wedding is measurable for the first time** — previously only the cricket folder could be scored, because it happens to put one person per directory. Append-only journal, so Adarsh and Devesh can write at once and an undo is a new line, not an edit. · `wff/web/feedback.py` · `verified`
- ✅ 4 ➕ **Judge mode** — two crops, `S`/`D` keys, ~2s each. Pairs are **not random**: they're drawn from the merge boundary (`pass2 ± 0.2`), where an answer actually moves the setting. On `ev_manali01` it surfaced 18 pairs, closest at **0.611** — precisely the empirical ceiling measured by hand earlier that day. Feeds a suggested threshold that errs toward splitting. · `wff/web/templates/judge.html` · `verified`
- ⚠ ➕ **Four real bugs found by actually clicking the thing, all fixed.** (a) `window.WFF` was defined *after* `app.js` loaded, so every request went to `/api/events/undefined/` — the whole feedback loop was dead and no test caught it because tests call the API directly. (b) An unknown event id returned Flask's HTML 500, which the browser then failed to parse as JSON, showing the reviewer `Unexpected token '<'` instead of a sentence. (c) Flagging one face inside an approved pile **silently destroyed the entire approval** (241 judged faces → 0); it now means "all one person *except this one*" and keeps the rest. (d) The worst: **pair counts scale with the square of a pile**, so one misplaced face reported as "240 wrongly merged", and the same lopsidedness (28,680 same-pairs vs 240 diff-pairs) drove the suggested threshold to **1.00 — which would merge every guest at the wedding into one person.** Scoring now reports **faces**, and the threshold sweep compares **rates**. · `verified`
- ✅ ➕ 50 new tests (104 total, all green), aimed at the silent failures: judgements surviving a re-clustering that renumbers people, two leftovers not counted as "grouped together", contradictions reported rather than resolved, a torn journal line not losing earlier judgements, path traversal on the image route, and both quadratic-scaling bugs locked down with the real numbers that produced them. · `tests/test_feedback.py`, `tests/test_web.py` · `verified`
- ⏸ **Devesh can't reach it yet.** It binds to `127.0.0.1` deliberately — face data is biometric data under the DPDP Act, so it isn't going on a network because a flag was easy to add. For now Devesh runs his own copy on his own photos and they swap `feedback.jsonl` files (the journal is designed to merge). A shared instance needs a decision from Adarsh first.
- ⚠ The judge screen shows **pile-to-pile** distance while the threshold evidence measures **face-to-face** — two different quantities. Both are labelled now, but the suggested number is guidance on where the boundary between people sits, *not* a value to paste into the merge knob. Worth resolving properly if threshold tuning becomes the main activity.

### 12 · Home page rebuilt — wireframe C, and run timing
**Agenda:** 1) Five light-theme wireframes of the home screen for Adarsh to choose from 2) Build the chosen one — C, "numbers first", indigo 3) Record the total time every run takes, from now on, for every run

### 12 · Review page — wireframes only, no code
**Agenda (from Adarsh, 23:43):** 1) Stay inside the "numbers first" theme already chosen 2) Re-lay-out the review/edit page — it is supercluttered, needs dedicated sections 3) Explain every number and every control in plain language; where an input is asked for, explain first then ask 4) Redesign the progress screen — name every step, show faces populating as photos are read 5) **Do not implement** — 4–5 wireframes for Adarsh to choose from

- ✅ 1–5 One artifact delivered: five layouts of the review page (A one-page-three-questions · B faces-first-with-docked-controls · C four tabs · D collapsed sections · E contents rail), three designs for the tuning controls, two for the progress screen, side-by-side comparison and a recommendation (B + preset controls + live-faces progress). Palette/type identical to the home-screen wireframe C. **Nothing implemented, no product code touched.** · `—` · `verified`
- ✅ 3 ➕ **Wrote the plain-language explanation of the thresholds that did not exist anywhere.** One idea — every face becomes a fingerprint, two fingerprints compare to a number 0.00–1.00 — then each knob renamed in real words ("start a pile only when two faces are this alike" / "join two piles when they're this alike" / "someone needs at least 3 photos to get their own pile") with the *guest-facing consequence* of moving it. Includes an SVG scale plotting the measured bands from `ev_manali01` (same person 0.29–0.45, different people ≥0.61) with 0.42/0.55 marked in the gap between them. Reusable as UI copy and as the answer to "what the heck are those". · `—` · `verified`
- ✅ 2 ➕ Design rule proposed for the rebuild: **if a number can't change a decision Adarsh would make, it doesn't go at the top of the page.** Concretely demotes "tight groups", "merges made" and the settings/env-var table into a collapsed "technical detail" section — they describe how the machine got there, not whether it is right.
- ➕ New idea worth keeping: the three presets (Careful / Balanced / Generous) can each show **the people/leftover counts they would produce** before you commit, because a re-group costs ~0.26s. Turns an unanswerable question into a visible comparison. Needs three clusterings per page load — cheap, but it is a real implementation cost to budget.
- ⏸ Awaiting Adarsh's pick (layout + control style + progress style) before any code. Note when it starts: the home-page rebuild chat holds `wff/web/templates/base.html`, `static/app.css`, `static/app.js` and `wff/web/app.py` — the review-page work overlaps all four, so it must wait for that claim to release or be split by file.

- ✅ 2 **Home page rebuilt as wireframe C, "numbers first".** Indigo (`#4B3F9E` accent, `#ECEBF3` band) exactly as picked. Project health reads first — 46px numbers for photos processed, faces found, speed, machine time, people — then the start card lifted over the band, then guidance, then history. Type scale raised throughout (body 15→16px, section headings 16→21px); the two complaints were small fonts and no distinction between sections. Verified in-browser against the real store, no horizontal overflow at 375px or desktop. · `wff/web/templates/{base,index}.html`, `wff/web/static/app.css` · `verified`
- ✅ 3 **Total time is now recorded for every run** — `wff/runs.py` + a per-event `runs.jsonl`. It used to be computed while a run went, printed once, and thrown away on exit, so "how long did Manali take?" had no answer the next morning. Both wall-clock total and Stage-1-only time are journalled on **every** ending (done, failed, stopped) from the console *and* the CLI (`--reviewer`) — recording only console runs would have left terminal work missing from every total. A resumed run contributes its real waiting time to machine time but **no speed sample**, so skipping 851 photos in 2s cannot drag the forecast to nonsense. The six pre-existing folders read **"not recorded"** rather than being back-filled with a guess. · `wff/runs.py`, `wff/web/jobs.py`, `wff/cli.py` · `verified`
- ✅ ➕ Identity: asked once on first open (Adarsh / Devesh / someone else), remembered, stamped on every run and judgement. · `wff/web/templates/base.html`, `wff/web/static/app.js` · `verified`
- ✅ ➕ **The dashboard's guidance is derived, not written.** It reads the real distribution — 642 of 1,202 face-bearing photos hold exactly one face, most in any single photo is 15 — and each tip disappears once dealt with; the "open 6 folders to count the people" nudge vanished the moment the backfill ran. Also added "same folder processed twice" detection via `source_folder_id`, so 1,765 photos processed is reported as **914 distinct**, and a `too_small_to_judge` state so Rishikesh-mini reads "too small to tell" instead of "0 people". · `wff/web/data.py` · `verified`
- ⚠ ➕ **Four real bugs found by running it, all fixed.** (a) `_record_run` read `job.photos_processed`, a field that does not exist — **every run silently failed to record while reporting success**. (b) A job read as *finished* before its timing was written, so a page reloading on that boundary showed "not recorded" for a timed run, and stopping the server there lost the timing permanently — exactly the failure the feature exists to prevent; `finished` now requires `recorded`. (c) A run that failed in 3 ms rounded to `0.0s` and rendered as "not recorded", indistinguishable from never-measured — floored to 0.01s, reads "under 1s". (d) `/api/forecast` rebuilt the entire dashboard on every keystroke (~600 ms), so the figure on screen lagged a character behind what was typed. · `verified`
- ✅ ➕ Fixing (d) exposed `list_keys` walking ~10,000 crops and previews to learn six event names. Added `ObjectStore.list_prefixes` — delimiter listing, which is what S3/R2 do natively with `Delimiter='/'`, with a base-class fallback so a backend only overrides it if it can do better. **Forecast 600 ms → 1 ms; home page 2.86s → 1.15s.** · `wff/storage/{base,local}.py` · `verified`
- ✅ ➕ 26 new tests (**130 total, green**), aimed at the ways a duration could be lost or quietly wrong: a resumed run reporting no speed, throughput weighted by photos so a 3-photo run cannot swing the forecast, a failed run still costing machine time, a torn journal line not losing earlier runs, and a reflection test over `_record_run` that would have caught bug (a) immediately. · `tests/test_runs.py`, `tests/test_storage.py` · `verified`
- ⚠ The forecast is only as representative as what has been timed. With one small fast folder recorded it will read optimistically until a full-size run lands — photo-weighting means a real 851-photo run corrects it immediately.

### 13 · Review page rebuilt — layout B, and the sliders removed
**Agenda (Adarsh, after the wireframes):** 1) Answer why a human is choosing the threshold at all — if it isn't needed, hide it and handle it in the backend 2) Build wireframe **B** (faces first, controls docked) 3) Build the **live-faces progress screen**

- ✅ 1 **The thresholds now choose themselves, and the sliders are gone from the page.** Adarsh's point was right: nobody can answer "how alike must two faces be?", so asking was a design failure. New `wff/cluster/autotune.py` measures it instead, using evidence every folder already carries: **two faces in one photograph are never the same person**, which hands us thousands of labelled *different-people* pairs per folder for free. Measure how close the closest of those get, discount it to pile-scale, draw the line below. On `ev_manali01`: 1,246 known-different pairs, closest 1% at 0.667 → **0.55**, which is exactly the value two people had arrived at by hand on 12 Aug, and reproduces the same 20 people / 155 leftovers. The machine now derives the number we guessed. · `wff/cluster/autotune.py`, `wff/web/{app,data}.py` · `verified`
- ⚠ 1 **The first version of that rule was wrong and would have merged real people.** It subtracted a fixed 0.06 from the measured face-to-face distance and landed on **0.61** for Manali — but 0.611 is where two genuinely different *people* sit there, so it would have merged them on the very folder it was measured from. Cause: face-to-face and pile-to-pile are different quantities (a pile is an average, and averaging cancels per-crop noise, so two people's pile-centres sit closer than any two of their faces). Fixed with a multiplicative discount (0.82) calibrated on that folder, clamped to 0.40–0.62, with the single calibration point written into the code and locked by a test. **Caught only because the number was checked against the hand measurement — the page would have looked perfectly reasonable.** · `verified`
- ✅ 1 Duplicate detections are excluded from the measurement: a face found twice in one photo reads as "two different people 0.02 apart" and would drag the threshold to the floor, splitting every guest. Filtered below 0.20, counted, and surfaced as a note — 1 such pair still exists in `ev_manali01`. Hand judgements are folded into the same measurement rather than a second mechanism. Manual sliders survive behind `?expert=1` for us only. · `verified`
- ✅ 2 **Review page rebuilt as wireframe B.** Piles are the page; the score, the grouping explanation and the jump list live in a docked panel that stays in the eyeline while scrolling (verified pinned at 2,500px scroll). Every number carries a sentence saying what it is; "tight groups", "merges made", the size-band chart, the settings table and the env vars moved into collapsed drawers, because none of them can change a decision Adarsh would make. Full page still renders **all 1,182 faces** of Manali, no truncation, no horizontal overflow at 375px. · `wff/web/templates/event.html`, `wff/web/static/{app.css,app.js}` · `verified`
- ✅ 3 **Live-faces progress screen.** All six steps named in plain words ("wake up the recogniser", "look at each photo") with the current one marked, and **crops appear on the page as they are found** — proven on a real 34-photo re-run: 4 faces on screen while 2 photos were done, ending 75 detected / 61 accepted, identical to the original `roka` run. New `LiveTail` follows `faces.jsonl` by byte offset rather than re-reading it (that file is tens of MB by the end of a wedding), carrying a torn final line forward — both locked by tests. One endpoint serves steps + faces + counters so they can never disagree on screen. · `wff/web/data.py`, `wff/web/app.py` · `verified`
- ⚠ ➕ Two rendering bugs found by using it: the ETA was floored server-side and rounded in the browser, so "about 2 min left" sat directly above "about 3 min left"; and `applyScore` overwrote the class of whatever element wrapped the score, which in the new dock would have wiped its styling on the first click. Both fixed. · `verified`
- ✅ 20 new tests (**144 total, all green**), incl. the calibration lock, the duplicate-detection trap, the too-small-folder fallback, and the tail's double-count and torn-line cases. **Nothing committed.**
- ⏸ `python -m wff run` (the CLI) still groups at the fixed 0.55 — only the console picks the threshold automatically. Worth unifying before anything ships.

### 13 · Home page — cards, dates, scrollable history
**Agenda (Adarsh):** 1) Drop "What to do next" — too much text to read daily 2) Give every section a real card so they are visually distinct 3) Move recent runs directly under the search bar, in a proper scroll area that survives dozens of runs 4) Show the date of each run, newest first

- ✅ 1 **"What to do next" is gone, and so is the code behind it.** Adarsh: "too much text — we're gonna use it daily and not read this daily." Removed the section and the ~130 lines that generated it (`Suggestion`, `_suggestions`, `_crowd_evidence`, `_judgement_count`), which ran on every page load. The one line worth keeping — "5,000 photos takes about 3h 09m" — moved into the bottom strip of the start card, where it answers "can I start this now, or is it a tomorrow job?" · `wff/web/data.py`, `wff/web/templates/index.html` · `verified`
- ✅ 2 **Every block is the same card now** — header strip, body, 14px radius — on a faint violet-grey tint that starts flush against the metrics band. White cards on a white page was exactly the "no distinction" complaint. Run cards sit in a tinted well so each one reads as a separate object rather than as a list drawn on one sheet. "What the machine actually does" became a closed drawer: it's reference, not a daily read. · `wff/web/static/app.css` · `verified`
- ✅ 3 **Recent runs moved directly under the start box**, into a fixed-height scroll area (`min(62vh, 660px)`) with shadows at the edges that appear only when there is more to scroll to. The list only grows, and a page that gets taller every week pushes everything else off screen. Past three folders a name filter appears in the card header — client-side, nothing reloads. · `wff/web/templates/index.html`, `wff/web/static/{app.css,app.js}` · `verified`
- ✅ 4 **Dates, newest first.** New `human_when` renders the UTC journals on this machine's wall clock ("Today, 1:07 am" · "11 Aug 2026, 6:58 pm") — a run that finished at 9:51 pm must not read as 4:21 pm. New `EventSummary.last_run_at`/`.when` sorts the list by when a folder was last *worked on*, not when it was first ingested; sorting on the ingest date would bury a folder re-run this morning under folders nobody has touched. Which of the two dates it is lives in the tooltip, not on the line — otherwise all six pre-timing folders carry the same footnote six times. · `wff/runs.py`, `wff/web/data.py` · `verified`
- ⚠ ➕ **Two layout bugs found by measuring the rendered page rather than looking at it.** The start card's -52px lift margin collapsed out through the new tint wrapper and dragged the grey up over the band's bottom edge, cancelling the lift (fixed with `display: flow-root`); and the band's default 32px section margin left a strip of white between band and tint that read as a seam. Neither would have been obvious from a screenshot. · `verified`
- ✅ 4 new tests (**149 total, green**) covering the two things that rot silently: that a missing timestamp never becomes a fabricated date, and that the list is ordered by the same date it displays — including the case the sort exists for, an old folder re-run today. **Nothing committed.**
- ⚠ The home page takes ~2.1s to render with 6 folders — parquet reads, pre-existing, not caused by this change. Worth a look before the list gets long.
- ✅ ➕ **Chased the 2.1s load time: the page was never slow, the measurement was.** It serves in **21 ms**. `localhost` on this machine resolves to `::1` first, the server binds IPv4 only, and Windows takes **2.039s** to refuse the IPv6 connection before falling back — measured directly with a raw socket. Chrome hides it by racing both stacks (its own timing: 34 ms server think). Anything scripted pays it in full: `127.0.0.1` 21 ms vs `localhost` 2,070 ms, side by side. `wff ui` now prints and opens the `127.0.0.1` URL, with the reason in a comment so nobody tidies it back to the prettier one. · `wff/cli.py` · `verified`
- ⚠ ➕ **One real defect found while looking, and it was mine from an hour earlier.** Deleting "what to do next" left `board.counts` written and never read, so the dashboard re-read the photo parquet for every folder for nothing — and it was doing the same with the run journal, which predates today. Both now come off the summaries that already opened those files. 12 parquet reads → 6, 12 journal reads → 6, output identical (1,765 photos / 3,401 faces / 45 people, checked against the pre-change page). The test counts reads rather than timing them — a stopwatch on a 6-folder store measures the disk cache, not the code — and was proven to fail against the old version before being kept. **150 tests green.** · `wff/web/data.py`, `tests/test_web.py` · `verified`
- ✅ ➕ Measured the scaling rather than guessing: **~6.4 µs per photo plus ~1.2 ms per folder**, so one 4,000-photo wedding ≈ 27 ms, ten ≈ 270 ms, fifty ≈ 1.3s. Deliberately did **not** cache `state.jsonl`: it is read live to drive the progress bar during a run, which is precisely where a stale-cache bug would hide, and the numbers say it is not needed this side of fifty weddings. · `verified`

### 13 · Wireframes round 3 — runs list + review screen (design only)
**Agenda (Adarsh, 09:40):** *Home page* 1) sort runs biggest-first 2) size chip at the top next to "done", with a background colour 3) split into large/medium/small with subtle card colours + a legend 4) let the run be renamed 5) explain "before we tracked who" 6) is run timing implemented? 7) show who tested it. *Review screen* 8) the intro paragraph and "orange edge" are vague — cut or explain 9) the pile buttons are confusing 10) still cluttered 11) big review % at the top + a Review button opening a floating window 12) the whole right-hand panel is too much text; what are "Judge pairs" and "Export answers" for. **Wireframes first, no implementation.**

- ✅ 5,6 ➕ **Two of his questions had answers already in the code, so they became copy fixes rather than features.** "before we tracked who" = the six folders processed before 12 Aug, when identity started being recorded — it becomes a grey `?` chip reading *set who ran this*, clickable. Run timing was built yesterday and works on every ending; only those same six read "not recorded", because back-filling a guess would poison the "5,000 photos takes about…" forecast. · `—` · `verified`
- ✅ 1–4,7 Three home-page layouts drawn in the real palette: **M1** three headed size sections (recommended) · **M2** one biggest-first list with a 4px colour rail · **M3** filter tabs. All three carry the same card changes: size chip (photos first, GB second) beside the status pill, rename-in-place pencil, tester avatar (Adarsh indigo / Devesh teal), time taken + per photo. Size bands proposed as **large 500+ / medium 100–499 / small under 100** — by photos, not gigabytes, because photo count is what decides whether a folder costs you an afternoon. · `—` · `verified`
- ⚠ ➕ **Flagged a colour collision rather than shipping it:** teal for "medium" sits next to the green "done" pill. Offered a one-hue alternative (indigo at three strengths) as a switch he can call.
- ✅ 8–12 Three review-screen layouts: **R1** big "40% checked" strip + a full-screen review window, one pile at a time, Y/N keys (recommended — it is what he described) · **R2** the same page with a sticky progress strip and the dock deleted · **R3** inbox split, pile list left, one pile right. The "No" path is drawn too: the window turns into *tap the faces that aren't this person*. · `—` · `verified`
- ✅ 8 ➕ **"Orange edge" gets deleted, not explained.** It means the face was under 112px tall in the original — where nearly every mistake lives. A legend nobody memorises becomes a button: **"Show the risky ones (28%)"**, which dims the rest. "Red edge" only ever meant anything on the cricket folder; gone.
- ✅ 12 ➕ **Answered what Judge pairs and Export are for, and recommended demoting both.** Judge pairs existed to set the matching strictness by hand — the machine has derived that itself since yesterday, so it becomes an optional 30-second bonus round at the end of a review instead of a nav button. Export is the only way Adarsh's and Devesh's answers can be merged while they run separate copies, so it survives as *"Send my answers to Devesh"* in a ••• menu, and dies the day there is one shared console.
- ✅ 10 ➕ **Named the cause of the clutter instead of just moving boxes:** the page is a report and a tool at once. Every option splits them — checking becomes a job with a start and an end, the numbers become a result you glance at. The right-hand panel's fourteen lines reduce to nine words: *8 of 20 piles checked · 1 mistake found*. Precision/recall wording dropped: it answers a question he never asked, and reads "100%" when the honest answer is "nothing found yet".
- ✅ ➕ Copy rewritten as before/after pairs so the wording can be approved separately from the layout — the instruction paragraph deleted outright (the window's heading *is* the question), and the symbol buttons ("⇄ Also another pile…", "← Put the face here") replaced with sentences.
- ⏸ **Nothing implemented, no product code touched** — he asked for wireframes to choose from. When it starts it will overlap `wff/web/templates/{index,event}.html`, `static/{app.css,app.js}`, `wff/web/{app,data}.py` and probably `wff/runs.py` (rename + reviewer backfill need somewhere to live). No claims held right now, so it is clear to start on his word. · artifact: `https://claude.ai/code/artifact/8bfd376c-32f4-4700-927d-cf9cd378d8e6`

### 14 · Sharing the panel — crops only, and does it need Cloudflare or a database?
**Agenda (Adarsh):** 1) From an admin's point of view we only need to see the same faces to judge accuracy — check whether we can ship just the crops and drop the big previews entirely 2) Do we actually need Cloudflare, or is there another way? 3) How do all the dashboard numbers populate, and does that need a live database so we both always see the same thing?

- OK 1 **Crops alone are enough — proven, not assumed.** Built a copy of the store with every preview and thumbnail removed (**16.9 MB against 830 MB**) and ran the console against it. Home page came back byte-identical in content: 1,765 photos, 3,401 faces, 2,431 usable, 45 people, 14.4 GB. The Manali review page rendered **all 1,182 faces with 0 broken images**, still finding 20 people and 155 leftovers. Counted what the page actually requests: **1,182 of 1,182 images are crops — it never asks for a preview at all.** So this was never a trade-off; the previews were only ever for the click-through. · `verified`
- OK + **Two more things turned out to be dead weight.** `thumb/` (20.7 MB per folder) is requested by nothing in the product — not the panel, not the report. And `embeddings.f32` (2.3 MB) is not needed either: `faces.parquet` carries the same vectors, and grouping reproduced its exact numbers without it. Neither gets shared. · `verified`
- WARN 1 **The one casualty, stated honestly:** clicking a face to see the whole photo. The API still answers 200 with the right box coordinates and caption ("3024 x 4032, taken 2023-04-08 12:11, 1 face found here") — only the JPEG 404s, so the popup opens with a broken image over a correct caption. Acceptable while the job is judging accuracy; whoever holds the originals still gets the full view. · `verified`
- OK **`.gitignore` inverted to match.** Heavy half stays out (`prev/`, `thumb/`, `*.f32`, generated `*.html`), everything else goes in. Dry-run: **3,445 files, 16.9 MB tracked, 0 heavy files leaking through.** About 9 MB per new folder. · `.gitignore` · `verified`
- OK 2 **Cloudflare is not needed.** Devesh pulls and runs `wff ui` and sees Adarsh's exact panel — no server running, no account, works offline, and git dates and attributes every change for free. A tunnel buys exactly one thing git cannot: watching a run happen live on the other person's machine. That is a want, not a need. Not building it.
- OK 3 **No database needed.** Every number on the dashboard comes from a small per-folder file — `state.jsonl` (photos done), `photos.parquet` (faces), `summary.json` (people), `event.json` (GB), `runs.jsonl` (timing) — read fresh on every page load in 21 ms. The only recomputed thing is the grouping itself, ~5s per folder on open, cached in memory after. A database would buy one thing: two people writing at once without collision.
- WARN **Known limit, recorded rather than designed around:** `feedback.jsonl` is append-only, so if both of us judge the *same* folder before pulling, git conflicts. Recoverable by keeping both sides, but it will be annoying the first time. Revisit when it actually bites.
- WARN **Face crops are biometric data under the DPDP Act.** This scheme is fine for our own photos in a private repo. It has to change before a real client's wedding folder lands in it. Written into `.gitignore` so nobody has to remember. **Nothing committed.**

### 13 · Built M1 + R1 — size-grouped runs, and a review window
**Agenda (Adarsh, after the wireframes):** "M1, R1, hide both."

- ✅ M1 **Runs are ordered biggest first and split into three headed groups** — large 500+, medium 100–499, small under 100, by *photos* rather than gigabytes because photo count is what decides whether a folder costs an afternoon. Group colours are deliberately weak (a 4px rail and a tint, not a filled card): green "done" and red "failed" already mean something on these cards, and a strong third colour would compete with the state you actually need to read. The legend only lists groups that exist, and the name filter now hides an empty heading — "no large runs" and "none matched what you typed" are different statements. · `wff/web/data.py`, `templates/index.html`, `static/app.css` · `verified`
- ✅ M1 **Size chip at the top of every card**, beside the state: "851 photos · 7.0 GB". Photos first because that is the number the sort, the group and the colour all mean. · `verified`
- ✅ M1 **Renaming, and fixing "before we tracked who".** New `wff/web/meta.py` stores a display name and a hand-set tester in `events/<id>/label.json` — *not* in `event.json` (Stage 1 owns that file) and *not* in the event id (renaming would orphan ~10,000 crops). Click the name, type, Enter; the id stays printed beside a renamed folder so nothing becomes unfindable on disk. The folders processed before the console asked for a name now read **"set who ran this"** and take one in the same way. A hand-set name beats the run journal, because typing one is a correction — and the journal itself is never rewritten. · `wff/web/meta.py`, `wff/web/app.py` · `verified`
- ⚠ M1 ➕ **One real bug, found by using it rather than by a test:** every POST from the browser carries a `reviewer` key naming whoever is at the keyboard, and the label route read the field it was *setting* from that same key — so **renaming a run silently reassigned it to whoever renamed it.** Renaming Manali reassigned it from Adarsh to me. The payload field is now `tested_by`, with a regression test that renames as one person and asserts the other still owns the run. · `verified`
- ✅ R1 **The review page is a status board; the checking happens in a window.** Top of the page: one number (**40% checked by you**), a bar, one line ("8 of 20 piles answered · 1 face fixed") and one button. Below it the piles as cards with a tick, clamped to two rows of crops. The docked panel of fourteen numbers — precision, recall, pairs merged, pairs split, "100% of your same person found" — is **gone**: it answered questions Adarsh never asked, and read "100%" when the honest answer was "nothing found yet". · `templates/event.html`, `static/{app.css,app.js}` · `verified`
- ✅ R1 **The window asks one question and takes two keys.** "Is everyone here the same person?" · **Y** yes · **N** someone else is in here · **→** skip · Escape closes. Pressing N turns the same window into *tap the faces that aren't this person*, and faces already marked wrong start selected, so one screen both adds and takes back. It advances to the next unanswered pile on its own and ends with "all 20 checked, 3 piles had somebody else in them". · `verified`
- ✅ R1 ➕ **The pile is moved into the window, not copied.** The page already holds every crop of every pile (all 1,182 on Manali, still untruncated); cloning 241 `<img>` tags to show the same faces again would have doubled the DOM of the biggest folder we have. A placeholder holds its position, and the pile list is captured once in page order — re-querying `#piles` would lose exactly the pile the window is showing, and with it "pile 9 of 20". · `verified`
- ✅ R1 ➕ **Order of writes in the fix flow is load-bearing.** Flag the wrong faces first, *then* approve the pile: the server leaves flagged faces out of an approval, so that records "all one person except these two" in one go. The other order — approve then flag — produced a contradiction the score would have reported. Verified end to end in the browser on a throwaway folder: journal reads `face_wrong` then `person_ok`, 0 contradictions, 3 same-pairs and 3 different-pairs known. **Those test judgements were then retracted**, so nobody's answer key carries my clicking. · `verified`
- ✅ 8 **"orange edge" is deleted, not explained.** It is replaced by a button — **"Show the risky ones (3%)"** — that dims everything else. It also now means what the caption says: under **112px** tall in the original (the size the recogniser wants), not the `second_class` flag, which is 80px and would have made the label a lie. · `verified`
- ✅ 12 **Judge pairs and Export are hidden**, in a ••• menu, renamed to say what they are for: *"Quick check on tricky pairs — two faces at a time, about 30 seconds. Optional."* and *"Send my answers to Devesh — you each run your own copy, so this is how the two sets get merged."* Both are gone from the top bar. The manual controls and "leave a note" live there too. · `verified`
- ✅ **13 new tests (163 total, all green)**, aimed at what would rot silently: that the list is ordered by the size it displays, that an empty size group never promotes another to the top spot, that renaming does not rewrite who ran it, that a pile counts as answered only when the one question was actually answered — and specifically that **"not a face at all" does not count**, or a folder could read 100% checked without a single person having been looked at. **Nothing committed.**
- ⏸ The dimming itself (`body.risky`) could not be confirmed on screen: the browser pane in this session is not compositing, and computed-style reads there are unreliable — an `!important` probe rule failed to register either. The rule is in the stylesheet and matches the element; **worth one glance in a real browser.**
- ➕ `ev_manali01` is now named **"Manali trip"** and marked as run by Adarsh, as a live check of the rename. Both are one click to change.

### 13 · Google Drive link — why it failed in under a second
**Agenda (Adarsh):** 1) Pasted a real public Drive folder link into the console, run died instantly with "No Google API key" — say why. 2) Fix it.

- ✅ 1 **Not a bug in the Drive code — the key never existed on this machine.** Google will not hand over even a fully public folder to an anonymous program without an API key, and the program had none, so it stopped at the first check before touching the network. That is why it took 0.02s. **The folder itself is fine**: fetched with no cookies and no account, the link comes back as the folder, not a sign-in wall and not "request access" — so the sharing half of landmine #2 is now confirmed on real data. · `verified`
- ✅ 2 **The key now has a home outside the terminal.** It used to be read only from an environment variable, which dies with the window and is invisible to a console started from a shortcut — so even after setting it once, the next run would have failed the same way. `python -m wff set-key <key>` writes it to `~/.wff/settings.env` (outside the repo, `chmod 600`), read at import so the tuning numbers can live there too. A real environment variable still wins, for one-off overrides. A `.env` in the checkout works as well, and is gitignored. · `wff/config.py`, `wff/cli.py`, `.gitignore` · `verified`
- ✅ 2 **The refusal now tells you what to do.** "Set WFF_GOOGLE_API_KEY" named a variable and stopped; it now prints the four clicks that produce a key and the one command that saves it, and says in a line what the key can and cannot reach — it reads public folders, it is not a login, it cannot touch anyone's private files. The other four ways Google says no are each translated: Drive API not enabled → where the Enable button is; key restricted to a website → which setting blocks us; invalid key → make another; folder not public → press Share, "Anyone with the link". · `wff/ingest/gdrive.py` · `verified`
- ✅ 2 ➕ **A real bug found while reading the retry loop: a permanent 403 was retried like a traffic jam.** "Drive API is switched off" and "you are going too fast" both arrive as 403, and the loop treated both as throttling — six attempts over 63 seconds, ending in "Drive request failed after retries", with Google's one useful sentence never shown. Now only the rate-limit reasons back off; a permanent refusal returns immediately and is explained. Would have cost an hour of guessing on the first key with the wrong settings. · `verified`
- ✅ 2 ➕ **The API key can no longer leak into the run log.** Three paths called `raise_for_status()`, and requests puts the full URL in that message — the key is a query parameter in the URL, so it would have been written into the on-screen log, and from there into `runs.jsonl`. Replaced with explained errors; a test asserts the key does not appear. · `verified`
- ✅ 2 **The failure reads as steps in the console, not one squashed paragraph** — the callout keeps its line breaks. Checked in the live page against the real failing link: 7 lines, rendered as 7. · `wff/web/templates/event.html`, `static/app.css` · `verified`
- ✅ **14 new tests, 177 total, all green.** Aimed at the parts that fail quietly: that a saved key survives a fresh process, that an environment variable still beats the file, that a permanent 403 is not slept on, that a rate-limit 403 still is, and that the key never reaches an error string. **Nothing committed.**
- ⏸ **Still unproven, and it needs Adarsh:** no key exists yet, so the API half of the Drive path has never actually run. `wff set-key <key>` then `wff verify-drive <link>` closes open item #1. The test run I submitted to check the console was retracted from `runs.jsonl`; his 11:07 failure is left as it happened.
- ⚠ ➕ **Cosmetic, unfixed, unrelated:** the last line of the log says "total time not recorded" for a run that failed in under a millisecond, while `runs.jsonl` correctly floors it to 0.01s and reads "under 1s". The log line uses the unfloored value. Left alone — it is in `wff/web/jobs.py`, outside this task.

### 13 · The Google key belongs in the console, not a terminal
**Agenda (Adarsh, in his words):** "bro, but now we have a UI, we just wanna paste the link there and it should work rather than doing all this."

- ✅ **He was right, and the terminal step is gone.** Paste a Drive link, press Start, and if there is no key the console asks for it *in place* — the four clicks that produce one, a box, and a Save button — then **starts the run you already pressed Start on**, by itself. The link and the name you typed ride along in the redirect, so nothing is re-pasted. `python -m wff set-key` still exists for me and Devesh, but nothing in the console points at it any more. · `wff/web/app.py`, `templates/index.html`, `static/{app.js,app.css}` · `verified`
- ✅ **A Drive link with no key no longer starts a run that can only fail.** It used to queue one, fail it in 0.02s, and leave a failed run in the journal teaching nobody anything; now the request never becomes a job. A local folder is untouched by any of this — the key is a Drive thing and must never stand between a folder on disk and a run, which is its own test. · `verified`
- ✅ **The key is checked with Google before it is saved, against the very folder that was just pasted.** Saving first and testing later is how you end up with a broken key in a file you cannot see, failing every run with the message you thought you had just fixed. **The one exception is deliberate:** a 404 means Google *accepted* the key and then said the folder is private — the key is good, so it is kept and the folder problem is reported instead. Otherwise fixing the sharing would mean pasting the key again. · `verified`
- ✅ **Verified live against the real Google API, not a mock.** Typed a deliberately fake key into the real box: Google rejected it, the refusal came back through the whole chain in plain words, the input kept its text so a typo can be fixed, and **no settings file was written** (checked on disk). Then stubbed only the network *reply* to exercise the success branch — the server log shows `POST /runs 302` firing on its own straight after the save, which is the interrupted run restarting itself. · `verified`
- ✅ ➕ **The wording followed the surface.** Two messages still ended in "run: python -m wff set-key <the-key>" — in a box he had just been told to use instead of a terminal. The invalid-key message now says to check for a missing character and paste it again; the no-key message points at the box, with the terminal command demoted to a parenthetical for whoever is actually in a terminal. · `verified`
- ✅ **The quiet state is one line**, not a panel: a green dot and "Google Drive links are set up · change the key", or a grey dot and "needs a one-time setup". The key itself is never rendered back into the page — asserted by a test. · `verified`
- ✅ **9 new tests, 186 total, all green.** Nothing committed.
- ⏸ **Unchanged and still his to do:** an actual key has to be created in a Google account, which I cannot do. The difference is that it is now four clicks and one paste into the console, and the console checks it for him.
- ⚠ The red on a rejected key could not be confirmed by reading computed styles on that element — this session's browser pane serves a stale style for an element whose class changed after load (an inline `!important` probe failed on it too, while a fresh probe element answered correctly). Read on freshly-created elements the rules are right: `.keybad` → the palette red with `pre-wrap`, `.keygood` → the palette green. Same limitation recorded yesterday against `body.risky`; **worth one glance in a real browser.**

### 13 · "Why is this run so slow?" — the 6,933-photo iPhone folder
**Agenda (Adarsh):** 1) The UI run on `D:\Adarsh\Iphone Data\2025-10-16` is crawling vs earlier runs — is the console slower than the terminal? Diagnose. (Read-only; nothing stopped, nothing tuned.)

- ✅ 1 **The console is not the problem — tiling and 12 MP HEIC photos are.** Measured from the store's own file timestamps rather than guessing: `ev_manali01` (851 real iPhone photos, run from the **terminal**, tiling on) = **2.27 s/photo**; `ev_manali_notile`, the identical folder with tiling off = **0.97 s/photo**. The live console run = **2.66 s/photo**. So tiling costs ~1.3 s/photo (2.3×) and the console costs ~0.4 s/photo (~17%) — and part of that 17% is the photos, not the console: this folder is **5,037 HEIC of 6,933** (17.3 GB) versus Manali's 641. · `verified`
- ✅ 1 **Why tiling costs what it does, in numbers:** `det_size` 640 ÷ `tile_target_scale` 0.25 = 2,560 px max tile edge, so a 4032×3024 iPhone photo plans a **2×2 grid** — the detector runs **5 times per photo** (one whole-image pass + four tiles) instead of once. `min_side_for_tiling` is 1,600, so every iPhone photo in the folder tiles. Dropping `WFF_TILE_TARGET_SCALE` to 0.20 makes the grid 2×1 → **3 runs instead of 5**, roughly a third off the total, at the cost of back-row recall. Not changed — that is an accuracy decision, not a speed one.
- ✅ 1 **The machine is the other half.** 4 cores / 8 threads, **8 GB RAM**, and the pipeline only sustains **2.28 cores** — the rest of the wall clock is single-threaded Python (HEIC decode, EXIF rotate, crop/preview JPEG writes) that ONNX's threads cannot help with. A browser tab on the same 4 cores is a real competitor here in a way it would not be on a bigger machine.
- ✅ 1 ➕ **One genuine console-side inefficiency found, small but growing:** `count_processed` re-reads and re-parses **the whole of `state.jsonl`** every 1.5 s from the progress watcher — measured at 18.7 ms for the current 2,450 rows, so ~1.2% of a core now and ~3.5% by the end of a 6,933-photo folder. `LiveTail` already solved exactly this problem for `faces.jsonl` with a byte offset; the progress counter never got the same treatment. Left alone mid-run. · `wff/web/data.py:392`
- ⏸ **Not stopped, deliberately.** The run is resumable (`state.jsonl` is the authority), 34% done at diagnosis time, ETA ~2h30m. Restarting to change a setting costs the 2,400 photos only if the model version changes — it does not — but re-tuning tiling mid-folder would make the accuracy read on this folder meaningless.
- ⚠ **Cosmetic bug spotted in the screenshot:** the event header reads "0 of 0 photos processed · nothing left this machine" while the live panel below correctly shows 2,359 of 6,933. The header comes from the cached dashboard summary, which is only invalidated after consolidation. Unfixed, outside this task.

### 13 · The progress bar was taxing the run it was measuring
**Agenda (Adarsh):** 1) "i stopped implement the fix" — build the one console-side inefficiency found in the diagnosis above.

- ✅ 1 **`ProgressTail` — the progress counter now reads only the bytes that are new.** `count_processed` read the whole of `state.jsonl` and re-parsed every line, and the watcher called it **every 1.5 seconds for the length of the run**, so the cost was worst exactly when the run was longest. Same byte-offset trick `LiveTail` already used on the other journal, carrying a torn final line forward. Measured on his real 3,121-photo journal: **13.38 ms → 0.156 ms per poll, 86× cheaper**, and per hour of watching **32.1 s of CPU → 0.38 s**. Both ways return the identical `(3121, 0)`. · `wff/web/data.py`, `wff/web/jobs.py` · `verified`
- ✅ 1 **`count_processed` deliberately left in place.** It is right for the one-shot callers — the home page reads it once per folder per request — and a tail there would be a cache with nothing to invalidate it. The tail is created per job and shared with the watcher thread, so the initial "how much did the last run already do?" read *is* the tail's first read: no duplicate full pass, and the two threads cannot disagree about the count.
- ✅ 1 **It is a tail, not a cache** — every call still reads whatever landed since the last one, so it cannot answer stale. That was the objection recorded on 13 Aug 02:10 against caching `state.jsonl` ("read live to drive the progress bar, which is exactly where a stale-cache bug would hide"), and it is why this is an offset and not a TTL.
- ✅ 1 **6 new tests, 192 total, all green — and each new one was proven to fail first.** Three deliberate mutants were introduced and the suite run against each: dropping the partial-line carry → only the torn-line test failed; removing the decrement of a photo's previous status → the failed-then-done test and the agreement test failed; seeking to 0 instead of the offset → only the byte-count test failed (15,582 bytes read instead of 742). The agreement test pins the tail to `count_processed` line by line, because two implementations of one count will drift and that is the test that notices. · `tests/test_web.py`
- ✅ 1 **Verified on a real run, not only in tests.** The `JobRunner` wiring changed (the watcher takes the tail as an argument now) and no existing test reaches it — the one end-to-end job test fails at the link check, before the tail is created. So a real 6-photo run through the real runner: progress ticked 1→2→3→4→5, ETA counted down, final counters `(6, 0)` matched the whole-file count exactly, 13 faces detected, 5 accepted, 1 person. · `verified`
- ⚠ ➕ **Correction to this morning's diagnosis, and a real reporting bug behind it.** The finished run journals **5.56 s/photo** (17,367 s ÷ 3,121 photos), not the 2.66 I reported from the first two thirds. The extra is **not** the pipeline getting slower: two gaps of 2h13m and 21m sit in the middle of it — the machine idle, almost certainly asleep — and with those removed the rate is **2.60 s/photo**, flat from photo 0 to photo 3,121 (per-250 buckets: 2.21 → 3.00, no drift, no leak). **But `runs.jsonl` counts sleep as run time**, and that figure feeds the home page's speed number and the "5,000 photos takes about 3h 09m" forecast. One closed lid poisons every estimate the console makes afterwards. Not fixed — it is a separate decision about what "machine time" should mean. Flagged for Adarsh.
- ⏸ Not touched, from the same diagnosis: the header reading "0 of 0 photos processed" during a live run (stale dashboard cache), and the tiling/speed dial, which is an accuracy decision to take after looking at this folder's 245 people.
- **Nothing committed.**

### 13 · The run cards were a mess — declutter, then a full check before pushing
**Agenda (Adarsh):** 1) The Recent-runs cards are cluttered and messy, names are breaking apart, the screen width is wasted — make it simple, subtle, presentable. 2) Then a complete end-to-end check, working and looking, before we push.

- ✅ 1 **The name no longer falls apart.** "Manali" was rendering as three letters stacked down the card, and a 30-character folder id took four lines. Cause: the header row held five things (name, pencil, id, size chip, state) with the name set to `word-break: break-word`, so it was the only thing that could shrink — and it shrank to one letter wide. The name now has the top line to itself, on one line, cut with an ellipsis if it must be, with the full name on hover (and a test that keeps the hover text there — an ellipsis with nothing behind it is a name nobody can read). · `wff/web/templates/index.html`, `wff/web/static/app.css` · `verified`
- ✅ 1 **Four colour systems on one card became one.** Cards were tinted violet/teal/grey by size, with a coloured 4px rail, a coloured size chip *and* a coloured state pill. Everything is now a plain white card with a hairline border, and **colour means one thing only: the state** — green done, amber stopped, red failed, grey not processed. The size legend above the list is deleted; what it said now sits in the heading it explains ("Large runs · 500+ photos each · 4 folders · 9,604 photos"), so there is less on the page and nothing less known. · `verified`
- ✅ 1 **Card content cut from three rows of mixed things to four fixed rows.** Name + state · one quiet grey line (id, when, gigabytes) · four numbers · who ran it + the button. The numbers sit in the same four columns on every card, so they read straight down the grid. Duplication removed: the photo count was printed twice (chip *and* number); it is now the number, and where a run stopped short the label says so — "3,121 / of 6,933 photos". "not recorded" — the widest thing on the card, saying nothing — is now an em dash with the explanation on hover. · `verified`
- ✅ 1 **The width complaint was real and was three different widths.** The metrics band, the start box and the run list were capped differently, so on a wide screen the start box was 400px wider than the list under it and nothing lined up. One `--page: 1680px` for all of them (and the review page, which was 1500px). Measured at 1900px: all three start at x=103 and are exactly 1680 wide, **4 cards per row instead of 3**. · `verified`
- ✅ ➕ **A real layout bug found and fixed on the way — the run list scrolled sideways on a phone.** A bare `1fr` grid track grows to its widest item's min-content, and with the name now on one line that item was a 30-character folder id: the list was 465px wide inside a 342px box, dragging sideways under the thumb. `minmax(0, 1fr)` plus `min-width: 0` on the card. On phones the four numbers also go two-by-two and a long name pushes the state onto its own line rather than being cut. 375px: no horizontal scroll anywhere. · `verified`
- ✅ 2 **End to end on the real thing, not a mock: a folder was pasted into the box and run to completion.** 26 photos → live card appeared with a working progress bar (updating in place, 13/26, 50%) → finished → the card read "done · 26 photos · 148 faces · 3 people · 1m 00s took · 1.9s each" with the right name stamped on it. Two more runs before it exercised the same path. **The three test folders were then deleted and the store checked back to exactly where it started** — 9 folders, 3 runs, 4,925 photos. · `verified`
- ✅ 2 **Everything else on the page exercised.** Filter (types "man" → 2 cards, empties a heading, "zzz" → the no-match line, cleared → 9 back), rename in place (opens in the new header, Escape restores, box fits at 375px), identity ask, review page (1,182 crops, **0 broken images**, 20 piles). **Zero JavaScript errors, zero server errors, every product request 200** — the single 404 in the log was my own probe for an endpoint that does not exist. No element sticks out of its card at 1900 / 1600 / 1280 / 375, and no number overflows its column. · `verified`
- ✅ 2 **2 new tests, 194 total, all green**, both proven to fail first against deliberate mutants: delete the range text from the heading → the "a heading has to state its own boundary" test fails; delete the hover name → the truncation test fails. · `tests/test_web.py`
- ⚠ Two things left as they were, both pre-existing and outside this task: a folder mid-run reads "not processed" on its card while "Running now" shows it live, and `runs.jsonl` still counts a sleeping machine as run time (flagged in the session above).
- **Nothing committed — as always, Adarsh reviews and pushes.**

### 13 · Where the 2.27 s per photo actually goes
**Agenda:** 1) Review the whole image-filtering core for optimisation — can any basic principle make it faster?

- ✅ 1 **Profiled the real pipeline on his own iPhone photos rather than reasoning about it: 2,248 ms/photo, which reproduces the 2.27 s/photo measured this afternoon.** Breakdown per 4032×3024 HEIC: **detection 1,270 ms (56%)** across 5 detector passes (1 whole-image + 4 tiles), **HEIC decode 611 ms (27%)**, **embedding 228 ms (10%)** for 1.2 accepted faces, preview JPEG 85 ms, everything else 54 ms. · read-only
- ✅ 1 **The headline finding: a quarter of every detector pass is spent looking at black padding.** SCRFD is fed a 640×640 square canvas, but a 4:3 photo only fills 640×480 of it — the other 25% is zeros, convolved at full cost. The ONNX file's height and width are dynamic (only the *batch* is frozen at 1), so the square canvas is not required. **Measured over 16 real photos: 28.6% faster detection, and the same faces — 23 of 23 matched, worst bounding-box corner moved 0.986 px and worst landmark 0.375 px on a 4032px photo.** Worth ~363 ms/photo. · `wff/process/detect.py` `_forward` · **not implemented, review only**
- ✅ 1 **Second finding, one line: `pillow_heif.options.DECODE_THREADS` defaults to 4 on an 8-thread machine.** Measured across 1/2/4/6/8/12 threads on 5 real HEICs: 4 → 8 takes decode from 418 ms to 357 ms. Worth ~60 ms/photo. 12 is worse than 8. · `wff/process/loader.py` · **not implemented**
- ✅ 1 ➕ **Three plausible optimisations measured and DISPROVED — recording them so nobody spends a day on them later.** (a) *Overlapping HEIC decode with detection in a background thread saves 4%, not the ~25% it looks like on paper* — the CPU is already saturated, and a decode that takes 448 ms alone takes 1,062 ms when it shares cores with ONNX. (b) *Batching the embedder does nothing*: 131 ms for 1 face, 125 ms/face at batch 4, and **worse** at batch 8 (152 ms/face) and 16 (163 ms/face) — so the comment in `pipeline.py` claiming batching is "most of the difference between a 4-hour job and a 7-hour one" is **false on this hardware**. (c) *Cutting OpenCV's thread count to stop it fighting ONNX makes things worse* — a 12 MP INTER_AREA resize is 27 ms at 8 threads, 81 ms at 1. Leave it alone. · read-only
- ⚠ 1 **Flagged for when the real wedding folder lands: embedding is 130 ms per face and flat — it does not amortise.** His current folder averages 1.2 accepted faces per photo, so embedding is only 10% of the time. A wedding full of group shots at ~8 accepted faces per photo would be **~1.0 s/photo of embedding alone**, turning today's smallest big cost into a rival of detection. Not a bug; a scaling property worth knowing before the folder arrives, not after.
- ⚠ 1 **The detector's ONNX file has its batch dimension frozen at 1**, so the five passes per photo cannot be merged into one call without re-exporting the model. Closed off as an avenue.
- ✅ 1 Two smaller ones: `IMWRITE_JPEG_OPTIMIZE` on the preview costs 14 ms and saves 3 KB of 565 (0.5%) — pure waste; and `WFF_TILE_TARGET_SCALE` 0.25 → 0.20 drops a 4032×3024 photo from 5 detector passes to 3, which is the single largest lever available (~500 ms/photo) but is an **accuracy trade and must be A/B'd**, not just switched on.
- **No product code touched — review only, nothing committed.**

### 13 · Architecture walkthrough, model landscape, then free accuracy work
**Agenda:** 1) Plain-language architecture overview + diagram 2) Where can accuracy go
if we spend money — rate the model market 3) Do whatever is free right now

- ✅ Architecture overview — one diagram, three bands (expensive-once Stage 1 → the
  saved fingerprint file → cheap-forever Stage 2 → the unbuilt guest side), plus the
  answer to "is it AI or code": two frozen borrowed models (SCRFD + ArcFace), everything
  around them ordinary arithmetic. Nothing learns on its own. · chat only · `verified`
- ✅ Model market rated — current ArcFace R50 8/10, AdaFace 9/10 (aimed at our exact
  low-resolution problem), the pair 9.5/10, AWS Rekognition 5/10 (~₹350/wedding, and it
  does its own shrinking so we lose tiling), Azure 3/10 (identification is gated behind
  an approval process). Conclusion: nothing worth buying; the gains are free ones.
  · chat only · `verified`
- ✅ **Measured before building, and two of three planned changes changed as a result.**
  Ran every real folder through the current code first. The suspected "near-tie" problem
  in small-face assignment **does not exist** (0 cases anywhere) — dropped, unbuilt. The
  merge line was checked and is **safe**: on `ev-2025-10-16` (7,164 faces) only 20 of
  26,652 known-different pairs sit closer than 0.55, and autotune independently says 0.59.
  · scratchpad only
- ✅ **The real leak found: the small-face line.** `second_class_assign_threshold = 0.38`
  was a guess, and it is wrong in **both** directions. On the real wedding folder a small
  face and a person known to be someone else never get closer than **0.667**, so 0.38 was
  discarding back-row guests for nothing; on `ev_manali01` the same negatives reach 0.382
  and **2 of the 6 faces it assigned were provably the wrong person**. Now measured per
  folder from the same free evidence autotune already uses. **Wedding folder: 262 → 345
  small faces attached (+83, +32%), leftovers 1,055 → 972. Manali: line tightened
  0.38 → 0.30**, which is the safety direction. · `wff/cluster/autotune.py`,
  `wff/cluster/two_pass.py` · `verified`
- ✅ **Pass 1 had no same-photo rule at all** — only pass 2 did, so a group could be *born*
  holding two faces from one photograph and pass 2 would then build on the contradiction.
  Real: 3 such groups on the wedding folder, 2 on Manali, one surviving into a final person
  on each. Repaired by splitting only (best face first, each joins the nearest sub-group
  not already holding its photo). **Impossible faces in final people: 3 → 0 and 1 → 0.**
  · `wff/cluster/two_pass.py` · `verified`
- ✅ Pile averages are now weighted by face quality (0.5 + q, so the best face counts at
  most 3× the worst). A blurry back-row crop no longer steers the average that every merge
  decision and every future guest selfie is compared against. · `wff/cluster/two_pass.py`
- ➕ 11 new tests; **216 total, all green.** People counts stayed stable across every
  folder (243→244 on the wedding set), so none of this is quietly reshaping the output.
- ⏸ **Not done, needs one overnight re-run each:** mirror-checking every face at embed
  time, and denser tiling. Both free in money, both invalidate every existing fingerprint
  (model version bump). Adarsh's call.
- ⚠ `WFF_SECOND_CLASS_AUTO` and the quality-weight constant live in `two_pass.py` instead
  of `config.py` — that file was claimed by another chat window at 18:10. They are read via
  `getattr(config, ...)` so moving them later is a pure deletion. **Nothing committed.**

**Agenda (added mid-session):** 2) Implement the two free wins — the padding fix and the decode threads.

- ✅ 2 **The detector no longer pads photos out to a square.** `_forward` now pads only to the next multiple of the coarsest stride (32) instead of out to a full 640×640 — at most 31 wasted rows instead of 160 — and the anchor grid is read off the **real canvas** rather than off `det_size`. New `_pad_to_stride` helper; `stride_max` read from the model's own FPN shape rather than assumed, so a 5-level export (stride 128) still works. **A/B'd on the shipped code over 18 real photos, forcing the old square canvas back through the same code path: 24 faces old, 24 faces new, 24 of 24 matched, worst box corner 0.986 px, worst landmark 0.375 px, worst score change 0.008 — and 24.8% faster (1,216 ms → 914 ms of detection per photo).** · `wff/process/detect.py` · `verified`
- ✅ 2 **HEIC decode threads now match the machine instead of libheif's default of 4.** New `configure_decoder()` in the loader, called once per job by `PhotoProcessor`, capped at 8 because 12 threads measured *slower* than 8. Tunable via `WFF_DECODE_THREADS`, and safe when pillow-heif is absent. · `wff/process/loader.py`, `wff/process/pipeline.py`, `wff/config.py` · `verified`
- ✅ 2 **11 new tests, 205 green at the time (216 with the other window's), and every new test proven to fail first against a deliberate mutant.** Restoring the square canvas kills 4 of them; keeping the tight canvas but reading the anchor grid off `det_size` again kills the coordinate test **with a silently wrong answer — the face lands at 1512 px instead of 2016 px, no error raised**, which is exactly the failure this test exists to catch. Detector tests use a stand-in ONNX session so the suite stays model-free and offline. · `tests/test_detect_geometry.py`, `tests/test_loader.py`
- ✅ 2 **Ran the whole pipeline end to end on 34 real photos** into a throwaway store (his real `.wff-store` untouched and verified at 9 folders afterwards). Same 34 photos, **75 faces detected — identical to the run the old code produced.**
- ⚠ ➕ 2 **A real, pre-existing bug found by that run, NOT caused by this change and NOT fixed: the "turned away" quality gate is decided by numerical noise.** One face went from accepted to rejected. Its box, blur, detection score and size are identical to the last decimal; only the yaw estimate moved — **from −10.2° to −55.35°, a 45-degree swing, caused by landmarks differing by 0.000122 px, which is float32 rounding.** `estimate_yaw` is deterministic (same input → same output every time) but catastrophically ill-conditioned: nudging the landmarks by 0.3 px sweeps the answer across **−77° to −14°**, straddling the 50° gate. Root cause is `cv2.solvePnP` with `SOLVEPNP_EPNP` on 5 near-coplanar points (flatness ratio 0.35) — the configuration EPnP is documented to be unreliable on; `SOLVEPNP_SQPNP` agrees with EPnP on the clean input (−56.18 vs −57.21) and is the obvious candidate. **Blast radius measured across all 8 stored events: 1,288 of 13,408 faces (9.6%) sit within 15° of the gate**, i.e. one in ten faces is accepted or rejected on a coin-flip, and 12 of the 15 rejections in this 34-photo run were "turned away". On this particular face the *old* answer was the wrong one — the nose sits outside the eye span, so the face genuinely is turned. Left alone deliberately: fixing it changes which faces every existing event accepts, which is Adarsh's call, not a side effect of a speed change. · `wff/process/quality.py` · `blocked: needs a decision`
- **Nothing committed — Adarsh reviews and pushes.**

### 13 · README for Devesh, then the first real push
**Agenda:** 1) Write a README covering how to run this and anything else worth knowing 2) Commit and push everything

- ✅ 1 **`README.md` written for someone who has never seen the repo** — what the
  product is, what exists today (a local console + a CLI, no cloud, nothing
  uploaded), the exact five-minute setup, both ways to run it, and what the review
  page is *for*. Includes: the models download themselves (~275 MB, once); the
  Drive-link key and where it is stored; **~2.3 s/photo measured**, so ~30 min for
  850 photos and ~2.5 h for a 4,000-photo wedding, resumable; HEIC works; where
  files land; the five thresholds worth touching and the note that the console
  measures them itself; the repo layout; the four standing code rules; and both
  landmines stated plainly (DPDP + the non-commercial model weights). Verified
  against the code rather than from memory — every command, default and path read
  out of `wff/cli.py` and `wff/config.py`. · `README.md` · `verified`
- ⚠ ➕ 1 **The repo is PUBLIC, and `.gitignore` had been written on the assumption
  it was private.** Its own comment said so ("this works while these are our own
  photos in a private repo"). As it stood, this push would have put **13,473 face
  crops / 92 MB** — Manali, the roka, the 6,933-photo wedding folder, family — onto
  the public internet permanently. That is biometric data under the DPDP Act. Asked
  Adarsh; he chose to leave it out. `.wff-store/` is now excluded whole, with the
  reason and the exact rule to restore recorded in the file for the day this moves
  to a private repo. **Cost of the choice: Devesh runs the 10-minute setup on his
  own photos instead of pulling Adarsh's panel ready-made.** · `.gitignore` · `verified`
- ✅ 2 **216 tests green**, no API key or personal path anywhere in the committed
  code (scanned), 60 files, no store leaking. Committed and pushed to `main`.
  · whole repo · `pushed`
