# ACTIVE WORK — concurrency registry

Single source of truth for **who is editing what, right now**, across every
concurrently open chat window. Not in git (this folder sits outside the repo's
tracked content), so it never causes commit noise.

**Reads are free. You only ever claim files you intend to WRITE.**

---

## Claim protocol (every agent, every task)

**1 — Plan before touching anything.**
Produce the plan first: what the task is, and the explicit list of files/globs
you will write to. No claim without a plan; no edit without a claim.

**2 — Read this file.** Check your list against every row in *Active claims*.

**3 — Overlap?**
- **No overlap** → go to step 4.
- **Overlap** → do NOT edit the contested files. Then, in this order:
  a. Do every part of the task that does *not* touch them, claiming only those files.
  b. Tell the user plainly: *"`worker/embed.py` is claimed by C-… since 20:15 — I've
     done X and Y; the rest is waiting on that."*
  c. Offer to re-check. Re-check on request or after finishing the rest — never
     spin in a polling loop, and never edit a claimed file "quickly".

**4 — Write your claim row**, then **immediately re-read this file**. If a
conflicting row for the same file appeared with an *earlier* start time, delete
your row and treat it as an overlap (step 3). Earlier timestamp always wins;
tie → shorter Claim ID wins.

**5 — Work.** If the task runs long, refresh `Heartbeat` whenever you re-read
this file. If scope grows to new files, claim them the same way *before* editing.

**6 — On completion (or abandonment):** move the row to *Recently released* with
a one-line outcome, and append the matching entry to
[`WORKLOG.md`](WORKLOG.md). Releasing is part of "done" — a task is not
finished while its claim is still open.

**Stale claims:** a row whose Heartbeat is >2h old is *probably* a chat that was
closed mid-task. Do not silently take it. Ask the user.

**Claim ID format:** `C-YYYYMMDD-HHMM-<slug>` — e.g. `C-20260810-1830-worker-pipeline`.
Get the time with `Get-Date -Format "yyyy-MM-dd HH:mm"`.

**Claiming a directory** is allowed with a glob (`worker/**`) when a task
genuinely rewrites a subsystem — but prefer listing files.

---

## Active claims

| Claim ID | Started | Task (one line) | Files / globs claimed | Status | Heartbeat |
|---|---|---|---|---|---|

---

## Recently released (keep last 10, newest first)

| Claim ID | Released | Task | Outcome |
|---|---|---|---|
| C-20260813-1836-readme-push | 2026-08-13 18:52 | Write the README and make the first real push | **Both done — and the push nearly leaked 13,473 face crops.** `README.md` written for someone opening the repo cold: the product in a paragraph, what exists today (local console + CLI, no cloud), the five-minute setup, both ways to run it, and what the review page is *for*. Every command, default, path and threshold was **read out of `wff/cli.py` and `wff/config.py` rather than recalled** — models self-download (~275 MB, once, to `~/.wff/models/`), the Drive key lives in `~/.wff/settings.env`, ~2.3 s/photo so ~30 min for 850 photos and ~2.5 h for a wedding, resumable, HEIC fine, plus the five thresholds worth touching and the note that the console measures them itself. Both landmines stated plainly. ⚠ ➕ **The find: this repo is PUBLIC and `.gitignore` had been written assuming it was private — its own comment said "this works while these are our own photos in a private repo."** As it stood, `git add .` would have published **13,473 crops / 92 MB** (Manali, the roka, the 6,933-photo wedding folder, family) permanently and indexed — biometric data under the DPDP Act. Stopped and asked; Adarsh chose to leave it out. `.wff-store/` is now excluded whole, with the reason and the exact rule to restore written into the file for the day this moves to a private repo. Cost: Devesh does the 10-minute setup on his own photos rather than pulling a ready-made panel. Pre-flight before pushing: **216 tests green**, no real API key or personal path anywhere in the committed code (scanned for both), 60 files staged, 0 store files leaking. **Committed and pushed to `main`** — first push of the product code; the repo previously held only docs. · `README.md`, `.gitignore`, `.claude/WORKLOG.md` |
| C-20260813-1810-padding-decode | 2026-08-13 18:50 | Drop the square-canvas padding in the detector + raise HEIC decode threads | **Both shipped and A/B'd against the old behaviour on real photos.** The detector padded every photo out to a 640×640 square, so a 4:3 photo spent **a quarter of all five passes convolving black**; it now pads only to the next multiple of the coarsest stride (31 wasted rows instead of 160) and reads the anchor grid off the **real canvas** rather than off `det_size`. `stride_max` is read from the model's own FPN shape, so a 5-level export still works. Forcing the square canvas back through the shipped code path over 18 real photos: **24 faces old, 24 new, 24 of 24 matched**, worst box corner 0.986 px, worst landmark 0.375 px, worst score change 0.008, **24.8% faster (1,216 → 914 ms of detection per photo)**. Plus HEIC decode threads now match the machine instead of libheif's default of 4 (capped at 8 — 12 measured *slower*), via a new `configure_decoder()` called once per job. **11 new tests, 216 total all green, each proven to fail first against a mutant** — notably, keeping the tight canvas but reading anchors off `det_size` again puts the face at 1512 px instead of 2016 px **with no error raised**, which is the whole reason that test exists. Full pipeline run end to end on 34 real photos into a throwaway store: **75 faces, identical to the old code's run**; his real store verified untouched at 9 folders. ⚠ ➕ **A real pre-existing bug found by that run and deliberately NOT fixed: the "turned away" gate is decided by numerical noise.** One face flipped to rejected with its box, blur, score and size identical to the last decimal — only yaw moved, **−10.2° → −55.35°, from landmarks differing by 0.000122 px (float32 rounding)**. `estimate_yaw` is deterministic but catastrophically ill-conditioned: a 0.3 px nudge sweeps it across −77° to −14°, straddling the 50° gate. Cause is `solvePnP` + `SOLVEPNP_EPNP` on 5 near-coplanar points (flatness 0.35); `SQPNP` agrees on clean input and is the obvious candidate. **Measured across all 8 stored events: 1,288 of 13,408 faces (9.6%) sit within 15° of the gate.** On this face the *old* answer was the wrong one. Left for Adarsh because fixing it changes which faces every existing event accepts. **Nothing committed.** · `wff/process/{detect,loader,pipeline}.py`, `wff/config.py`, `tests/test_{detect_geometry,loader}.py` |
| C-20260813-1812-stage2-accuracy | 2026-08-13 18:47 | Free Stage-2-only accuracy work | **Measured every real folder BEFORE writing anything, and two of the three planned changes changed as a result.** The suspected near-tie problem in small-face assignment **does not exist** (0 cases on any folder) — dropped unbuilt. The merge line was checked and is **safe**: on `ev-2025-10-16` (7,164 faces) only 20 of 26,652 known-different pairs sit closer than 0.55, and autotune independently lands on 0.59. **The real leak was `second_class_assign_threshold = 0.38`**, a guess that is wrong in *both* directions — on the wedding folder a small face and a person known to be someone else never get closer than **0.667** (so it was discarding back-row guests for nothing), while on `ev_manali01` the same negatives reach 0.382 and **2 of the 6 faces it assigned were provably the wrong person**. Now measured per folder from the same free evidence autotune already uses: **wedding folder 262 → 345 small faces attached (+83, +32%), leftovers 1,055 → 972; Manali tightened 0.38 → 0.30**. Second find: **pass 1 had no same-photo rule at all**, so a group could be *born* holding two faces from one photograph and pass 2 would build on the contradiction — 3 such groups on the wedding folder, 2 on Manali, one surviving into a final person on each; repaired by splitting only, **impossible faces 3 → 0 and 1 → 0**. Third: pile averages are now weighted by face quality (0.5 + q, best face counts at most 3× the worst). People counts stayed stable everywhere (243→244), so nothing is quietly reshaping the output. 11 new tests, **216 total all green. Nothing committed.** ⚠ `WFF_SECOND_CLASS_AUTO` + the quality-weight constant live in `two_pass.py` rather than `config.py` (held by C-20260813-1810 at the time); read via `getattr(config, ...)` so moving them is a pure deletion. ⏸ Mirror-checking faces at embed time and denser tiling are still open — free in money, but each needs an overnight re-run. · `wff/cluster/{two_pass,autotune}.py`, `tests/test_{cluster,autotune}.py` |
| C-20260813-1754-perf-review | 2026-08-13 18:05 | Review the image-filtering core for speed optimisations | **Profiled rather than guessed, on his own iPhone photos: 2,248 ms/photo, reproducing this afternoon's 2.27 s.** Detection 56% (5 passes/photo), HEIC decode 27%, embedding 10%, preview 4%. **Headline: a quarter of every detector pass is convolving black padding** — SCRFD gets a 640×640 square canvas but a 4:3 photo fills only 640×480, and the ONNX file's H/W are dynamic (only batch is frozen at 1), so the square is not required. Measured over 16 real photos: **28.6% faster detection, 23 of 23 faces identical**, worst box corner 0.986 px and worst landmark 0.375 px. Plus one line: `pillow_heif` `DECODE_THREADS` defaults to 4 on an 8-thread machine; 8 saves ~60 ms/photo. Together ≈ **19% off every photo with no accuracy change**. ➕ **Three plausible ideas measured and disproved**, recorded so they are not re-attempted: overlapping decode with inference saves only 4% (CPU already saturated — a 448 ms decode becomes 1,062 ms when sharing cores); **embedder batching does nothing** (131 ms at batch 1, 125 at batch 4, *worse* at 8 and 16) so `pipeline.py`'s "difference between a 4-hour job and a 7-hour one" comment is false on this hardware; and cutting OpenCV threads makes resizes 3× slower. ⚠ Flagged, not fixed: embedding is **130 ms per face and flat**, so a group-shot-heavy wedding at ~8 faces/photo means ~1.0 s/photo of embedding alone — a scaling property worth knowing before the real folder lands. ⚠ Detector batch dim is frozen at 1, so the 5 passes cannot be merged without re-exporting the model. **No product code touched, nothing committed.** · `.claude/WORKLOG.md` only |
| C-20260813-1711-runcards-declutter | 2026-08-13 17:52 | Declutter the Recent-runs cards + full end-to-end check before push | **Done and driven in a real browser against the real 9-folder store.** The name gets the top line to itself and is cut with an ellipsis (full name on hover, kept by a test) instead of stacking one letter per line; **four colour systems on one card became one** — plain white cards, colour now means only the state — with the size legend deleted into the heading it explained ("Large runs · 500+ photos each"). Card content is four fixed rows and the four numbers sit in the same columns on every card; the photo count was printed twice and is now once, with "3,121 / of 6,933 photos" where a run stopped short, and "not recorded" (the widest thing on the card) is an em dash with the reason on hover. **The width complaint was three different widths**: band, start box and run list were capped 1500/none/1500 so nothing lined up — one `--page: 1680px` for all of them and the review page, measured at 1900px as x=103 × 1680 for all three, **4 cards per row instead of 3**. ➕ **Real bug found on the way: the run list scrolled sideways on a phone** — a bare `1fr` track grows to its widest item's min-content, which was now a 30-character folder id (465px inside a 342px box); `minmax(0, 1fr)` + `min-width: 0`, numbers two-by-two on phones, no horizontal scroll at 375px. **End-to-end proved on the real thing**: a folder pasted into the box → live card with a progress bar updating in place → finished card reading "done · 26 photos · 148 faces · 3 people · 1m 00s took · 1.9s each"; the three test folders were then deleted and the store verified back at exactly 9 folders / 3 runs / 4,925 photos. Filter, rename, identity ask and the review page (1,182 crops, 0 broken) all exercised: **0 JS errors, 0 server errors, every product request 200**, nothing spilling its card at 1900/1600/1280/375. 2 new tests, **194 total, all green**, both proven to fail first against mutants. **Nothing committed.** ⚠ Left alone, pre-existing: a folder mid-run reads "not processed" on its card, and `runs.jsonl` counts a sleeping machine as run time. · `wff/web/templates/index.html`, `wff/web/static/app.css`, `tests/test_web.py` |
| C-20260813-1352-progress-tail | 2026-08-13 17:05 | Stop the progress watcher re-reading all of state.jsonl every 1.5s | **Done and measured on his real journal: 13.38 ms → 0.156 ms per poll (86×), 32.1 s → 0.38 s of CPU per hour of watching**, same answer `(3121, 0)` both ways. New `ProgressTail` in `wff/web/data.py` follows `state.jsonl` by byte offset, carries a torn last line forward, and keeps status per photo so a retried photo cannot be counted twice. `count_processed` kept for the one-shot callers (home page); the tail is created per job and shared with the watcher thread, so the "what did the last run already do?" read *is* its first read — no duplicate pass, no disagreement between threads. It is a tail, not a cache: it can never answer stale, which was the standing objection to caching this file. **6 new tests, 192 total, all green, each proven to fail first against three deliberate mutants.** Also verified on a real 6-photo run through the real `JobRunner`, because the wiring change is not reachable from any existing test. ⚠ ➕ **Correction:** the finished run journals 5.56 s/photo, not the 2.66 reported this morning — but two idle gaps (2h13m + 21m, machine asleep) account for all of it; the working rate is **2.60 s/photo, flat across all 3,121 photos**. `runs.jsonl` counting sleep as run time poisons the console's speed number and forecast — flagged, not fixed. **Nothing committed.** · `wff/web/data.py`, `wff/web/jobs.py`, `tests/test_web.py` |
| C-20260813-1338-slow-run-diagnosis | 2026-08-13 13:42 | Why is the ev-2025-10-16 run so slow — UI vs CLI? | **The console is not slower in any way that matters; tiling and 12 MP HEIC are the cost.** Measured off the store's own timestamps: terminal run on 851 real iPhone photos with tiling = **2.27 s/photo**, same folder tiling off = **0.97 s/photo**, live console run = **2.66 s/photo**. So tiling is ~1.3 s/photo (2.3×) and the console is ~0.4 s/photo (~17%) — some of which is the folder itself (5,037 HEIC of 6,933, 17.3 GB, vs Manali's 641). A 4032×3024 photo plans a 2×2 grid, so the detector runs **5 times per photo**; `WFF_TILE_TARGET_SCALE=0.20` would make it 3. Machine is 4 cores / 8 GB and only sustains 2.28 cores — the rest is single-threaded HEIC decode and JPEG writes. ➕ One real console inefficiency found: `count_processed` re-parses all of `state.jsonl` every 1.5s (18.7 ms at 2,450 rows, ~3.5% of a core by the end) when `LiveTail`'s byte-offset trick already exists next to it. **Nothing changed, nothing stopped** — the run is resumable and 34% in. ⚠ Header reads "0 of 0 photos processed" during a live run (stale dashboard cache). · read-only; `.claude/WORKLOG.md` only |
| C-20260813-1145-key-in-ui | 2026-08-13 12:35 | "We have a UI, we just wanna paste the link there and it should work" | **The terminal step is gone.** Paste a Drive link, press Start, and if there is no key the console asks for it in place — the four clicks that produce one, a box, a Save button — then **starts the run he already pressed Start on**, by itself; the link and the name he typed ride along in the redirect so nothing is re-pasted. A Drive link with no key no longer queues a run that can only fail (it used to fail it in 0.02s and leave a failed run in the journal); a local folder is untouched, with its own test, because the key must never stand between a folder on disk and a run. **The key is checked with Google before it is saved**, against the very folder just pasted — saving first and testing later is how you end up with a broken key in a file you cannot see, failing every run with the message you thought you had fixed. One deliberate exception: a 404 means Google *accepted* the key and then said the folder is private, so the key is kept and the folder problem reported, or fixing the sharing would mean pasting the key again. **Verified live against the real Google API, not a mock**: a deliberately fake key was rejected, the refusal arrived in plain words, the input kept its text so a typo can be fixed, and no settings file was written (checked on disk); stubbing only the network *reply* then showed `POST /runs 302` firing on its own — the interrupted run restarting itself. ➕ Two messages still ended in "run: python -m wff set-key" *inside the box that replaces the terminal*; both rewritten, the command demoted to a parenthetical for whoever is genuinely in a terminal. Quiet state is one line (green dot "set up · change the key" / grey dot "needs a one-time setup"), and the key is never rendered back into the page — asserted by a test. 9 new tests, **186 total, all green. Nothing committed.** ⏸ Creating the key in a Google account is still his — it is now four clicks and one paste, and the console checks it. ⚠ The red on a rejected key could not be confirmed by computed style: this pane serves a stale style for an element whose class changed after load (an inline `!important` probe failed on it too, a fresh probe answered correctly); read on fresh elements the rules are right. Same limitation as yesterday's `body.risky`. · `wff/web/app.py`, `wff/web/templates/{index,event}.html`, `wff/web/static/{app.js,app.css}`, `wff/ingest/gdrive.py`, `tests/test_web.py` |
| C-20260813-1110-gdrive-key | 2026-08-13 11:34 | Drive link failed in 0.02s with "No Google API key" — explain and fix | **Not a code bug: Google will not hand over even a fully public folder to an anonymous program without an API key, and this machine had none, so the run stopped at the first check before touching the network.** The folder itself is fine — fetched with no cookies and no account it returns the folder, not a sign-in wall and not "request access", so the *sharing* half of landmine #2 is now confirmed on real data. Fixed the part that would have made it fail twice: the key was read **only** from an environment variable, which dies with the terminal window and is invisible to a console started from a shortcut. `python -m wff set-key <key>` now writes it to `~/.wff/settings.env` (outside the repo, chmod 600), read at import so tuning numbers can live there too; a real env var still wins for one-off overrides, and a gitignored `.env` in the checkout works as well. The refusal itself was rewritten: "Set WFF_GOOGLE_API_KEY" named a variable and stopped, and now prints the four clicks that produce a key, the one command that saves it, and what the key can and cannot reach. The other four ways Google says no are each translated (API not enabled → where Enable is · key restricted to a website → which setting blocks us · invalid key · folder not public → press Share). **Two real bugs found while reading the code:** (a) a *permanent* 403 was retried like a traffic jam — "Drive API is switched off" and "you are going too fast" both arrive as 403, so the loop spent 63s and reported a timeout while never showing Google's one useful sentence; only rate-limit reasons back off now. (b) Three paths called `raise_for_status()`, and requests puts the full URL in that message — **the API key is a query parameter in that URL**, so it would have landed in the on-screen log and in `runs.jsonl`; replaced with explained errors, with a test asserting the key never appears. Console callout now keeps its line breaks (verified live against the real failing link: 7 lines, rendered as 7). 14 new tests, **177 total, all green. Nothing committed.** ⏸ **The API half is still unproven** — no key exists yet, so `set-key` → `verify-drive` is Adarsh's step and is what closes open item #1. My test run was retracted from `runs.jsonl`; his 11:07 failure left as it happened. · `wff/config.py`, `wff/cli.py`, `wff/ingest/gdrive.py`, `tests/test_gdrive.py`, `.gitignore`, `wff/web/templates/event.html`, `wff/web/static/app.css` |
| C-20260813-1020-m1r1-build | 2026-08-13 11:04 | Build M1 (home: biggest-first size groups, size chip, rename, tester) + R1 (review: big %, review window), hide Judge pairs & Export | **Both built and driven in a real browser against the real store.** *Home:* runs ordered biggest first in three headed groups (large 500+ / medium 100–499 / small under 100, by photos not gigabytes), size chip beside the state, colours kept weak on purpose so they cannot compete with the green "done" and red "failed" pills, legend lists only groups that exist, and the name filter hides an emptied heading. New `wff/web/meta.py` adds a rename and a hand-set tester in `events/<id>/label.json` — never in `event.json` (Stage 1 owns it) and never in the id (renaming would orphan ~10,000 crops); the id stays printed beside a renamed folder, and "before we tracked who" became **"set who ran this"**. *Review:* the docked panel of fourteen numbers is gone, replaced by **one percentage, one line, one button**; the checking happens in a window that shows one pile and asks one question, **Y**/**N**/**→**, advancing on its own and ending with a summary. The pile is *moved* into the window rather than cloned (the page already holds all 1,182 Manali crops). "orange edge" deleted in favour of **"Show the risky ones (3%)"**, and it now means what the caption says — under 112px, not the 80px `second_class` flag, which would have made the label a lie. Judge pairs + Export demoted into a ••• menu, renamed to say what they are for. **One real bug found by using it:** every browser POST carries a `reviewer` key naming whoever is at the keyboard, and the label route read the field it was *setting* from that same key — **renaming a run silently reassigned it to whoever renamed it**; now `tested_by`, with a regression test. Fix-flow write order is load-bearing (flag first, approve second) and was verified against the journal: 0 contradictions; those test judgements were then retracted. 13 new tests, **163 total, all green. Nothing committed.** ⏸ The `body.risky` dimming could not be confirmed on screen — this session's browser pane does not composite and computed-style reads there are unreliable (an `!important` probe failed too); the rule is present and matches, but it wants one glance. · `wff/web/{meta,data,app}.py`, `templates/{index,event}.html`, `static/{app.css,app.js}`, `tests/test_web.py` |
| C-20260813-1014-crops-only-share | 2026-08-13 10:26 | Can the panel run on crops alone, and does sharing it need Cloudflare or a database? | **Yes to crops-only, no to both.** Built a copy of the store with every preview and thumbnail stripped out (**16.9 MB vs 830 MB**) and ran the console against it: home page identical to the real thing — 1,765 photos / 3,401 faces / 2,431 usable / 45 people / 14.4 GB — and the Manali review page rendered **all 1,182 faces, 0 broken images**, still finding 20 people and 155 leftovers. Counted every image the review page requests: **1,182 of 1,182 are crops, it never asks for a preview at all.** `thumb/` is requested by nothing in the product (20.7 MB/folder of dead weight) and `embeddings.f32` is not needed either — `faces.parquet` carries the same vectors and clustering reproduced its numbers without it. The single casualty is click-a-face-to-see-the-photo: the API still returns 200 with correct box coordinates and caption ("3024 x 4032 · taken 2023-04-08 12:11"), only the JPEG 404s, so the popup opens with a broken image and a right caption. `.gitignore` inverted accordingly — heavy half (`prev/`, `thumb/`, `*.f32`, generated `*.html`) stays out, everything else goes in. Dry-run confirms **3,445 files / 16.9 MB tracked, 0 heavy files leaking**. So **Cloudflare is not needed**: Devesh pulls and runs `wff ui` and sees Adarsh's exact panel, no server, no account, offline. A tunnel only buys watching a run live, which is a want, not a need. **No database needed either** — every number comes from a small per-folder file (`state.jsonl` → photos, `photos.parquet` → faces, `summary.json` → people, `event.json` → GB, `runs.jsonl` → timing) or is recomputed in ~5s from the parquet. Known limit recorded, not designed around: `feedback.jsonl` is append-only so simultaneous judging of the *same* folder can conflict in git. ⚠ Face crops are biometric data — this scheme holds for our own photos in a private repo and must change before a client's wedding lands in it. **Nothing committed.** · `.gitignore`, `.claude/launch.json` |
| C-20260813-0946-wireframes-r3 | 2026-08-13 10:12 | Wireframes for the runs list + review screen | Design only. Three home-page layouts (M1 headed size sections · M2 one list with colour rail · M3 tabs) and three review layouts (R1 big % + review window · R2 sticky strip · R3 inbox split), all in the real product palette, plus the card anatomy, the review window's yes/no paths, and every piece of copy rewritten before/after. Two of Adarsh's questions turned out to be already-answered: run timing was built yesterday, and "before we tracked who" is the six pre-12-Aug folders. Recommended deleting the "orange edge" legend in favour of a *Show the risky ones (28%)* button, and demoting Judge pairs + Export rather than keeping them in the nav. **No product code touched**; when implementation starts it overlaps `wff/web/templates/{index,event}.html`, `static/{app.css,app.js}`, `wff/web/{app,data}.py`. · `.claude/WORKLOG.md`, artifact only |
| C-20260813-0152-home-load-time | 2026-08-13 02:10 | Chase the home page's 2.1s load time | **The page was never slow — the measurement was.** It serves in **21 ms**; the 2.1s was my own timing probe. `localhost` on this machine resolves to `::1` first, the server binds IPv4 only, and Windows takes **2.039s** to refuse the IPv6 connection before falling back (measured directly with a raw socket). Chrome hides it by racing both stacks — the browser's own navigation timing says 34 ms of server think, 352 ms to full response. Anything scripted pays it in full: `127.0.0.1` 21 ms vs `localhost` 2,070 ms, side by side. `wff ui` now prints and opens the `127.0.0.1` URL rather than the prettier `localhost`, with the reason in a comment so nobody tidies it back. **One real defect found while looking, and it was mine**: deleting the "what to do next" section left `board.counts` written and never read, so `build_dashboard` re-read the photo parquet for every folder for nothing — and it was doing the same with the run journal, which predates today. Both now come off the summaries that already opened those files; `EventSummary` carries its own `runs`. 12 parquet reads → 6, 12 journal reads → 6, identical output (1,765 photos / 3,401 faces / 2,431 accepted / 45 people, verified byte-for-byte against the pre-change page). A test counts the reads rather than timing them — a stopwatch on a 6-folder store measures the disk cache — and it was proven to fail against the old code before being kept. **Measured the scaling instead of guessing at it**: ~6.4 µs per photo plus ~1.2 ms per folder, so one 4,000-photo wedding ≈ 27 ms, ten ≈ 270 ms, fifty ≈ 1.3 s. Deliberately did **not** add caching for state.jsonl: it is read live to drive the progress bar during a run, which is exactly where a stale-cache bug would hide, and the numbers say it is not needed this side of fifty weddings. **150 tests green. Nothing committed.** · `wff/web/data.py`, `wff/cli.py`, `tests/test_web.py` |
| C-20260813-0118-home-cards-dates | 2026-08-13 01:42 | Home page: drop "what to do next", card structure, runs under the search bar in a scroll area, dates newest first | All four asks done and verified in-browser against the real 6-folder store. **"What to do next" is gone**, and so is the code behind it — `Suggestion`, `_suggestions`, `_crowd_evidence`, `_judgement_count`, ~130 lines that were computed on every page load for a section nobody was going to read (the per-request cost it saves is only 3 ms; the point is the reading, not the speed). The one line worth keeping, the "5,000 photos takes about 3h 09m" estimate, moved into the bottom strip of the start card. **Every block is now the same card shape** — header strip, body, 14px radius — sitting on a faint violet-grey tint (`#f6f5fa`) that starts flush against the band, because white cards on a white page were the "no distinction between sections" complaint. Run cards inside a tinted well (`#f8f7fc`) so each reads as a separate object. **The runs moved directly under the start box** into a fixed-height scroll area (`min(62vh, 660px)`) with Lea Verou scroll shadows, plus a client-side name filter that appears past three folders — the list only ever grows, and a page that gets taller every week pushes everything else off screen. **Dates**: new `human_when` in `wff/runs.py` renders UTC journals on the local wall clock ("Today, 1:07 am" · "11 Aug 2026, 6:58 pm"), and `EventSummary.last_run_at` / `.when` puts the list in true most-recently-worked-on order — sorting on ingest date would bury a folder re-run this morning under folders nobody has touched. Which date it is lives in the tooltip, not on the line, or all six pre-timing folders would carry the same footnote six times. **Two layout bugs found by measuring rather than looking**: the start card's -52px lift margin collapsed out through the new tint wrapper and dragged the grey up over the band's bottom edge (fixed with `display: flow-root`), and the band's default 32px section margin left a strip of white reading as a seam between band and tint. No horizontal overflow at 375px or desktop; scroll area, filter, and the drawer all exercised in the live page. 4 new tests, **149 total, all green**. **Nothing committed.** ⚠ The home page takes ~2.1s to render with 6 folders — parquet reads, pre-existing, not from this change, but worth a look before the list gets long. · `wff/web/templates/index.html`, `wff/web/static/{app.css,app.js}`, `wff/web/{data,app}.py`, `wff/runs.py`, `tests/{test_web,test_runs}.py`, `.claude/launch.json` |
| C-20260813-0040-review-b-layout | 2026-08-13 01:55 | Review page as wireframe B, live-faces progress, thresholds chosen automatically | **The sliders are gone — the threshold measures itself.** New `wff/cluster/autotune.py` uses the evidence every folder already carries: two faces in one photo are never the same person, so a folder labels its own negatives for free. On `ev_manali01` that is 1,246 known-different pairs, closest 1% at 0.667 → **0.55**, the exact value arrived at by hand on 12 Aug, reproducing 20 people / 155 leftovers. **The first version of the rule was wrong and would have merged real people** (fixed 0.06 subtraction → 0.61, and 0.611 is where two different people sit on that folder): face-to-face and pile-to-pile are different quantities, because a pile is an average and averaging cancels per-crop noise. Now a multiplicative 0.82 discount, clamped 0.40–0.62, its single calibration point documented in the code and locked by a test. Duplicate detections (a face found twice, reading as "different people 0.02 apart") are excluded, counted and surfaced — one still exists in Manali. Hand judgements fold into the same measurement. Sliders survive behind `?expert=1` for us. Review page rebuilt as **wireframe B**: piles are the page, score + grouping explanation + jump list docked and pinned (verified at 2,500px scroll), plumbing numbers demoted to drawers, all 1,182 Manali faces still rendered, no overflow at 375px. **Live-faces progress**: six steps in plain words, crops landing as they are found — proven on a real 34-photo re-run (4 faces on screen at 2 photos done; 75/61 at the end, identical to the original). `LiveTail` follows `faces.jsonl` by byte offset instead of re-reading tens of MB per poll, carrying torn lines forward. Two rendering bugs found by using it (ETA floored server-side vs rounded in the browser; `applyScore` wiping the class of its container). 20 new tests, **145 total, all green**. **Nothing committed.** ⏸ The CLI still groups at the fixed 0.55 — only the console auto-tunes. · `wff/cluster/autotune.py`, `wff/web/{app,data}.py`, `wff/web/templates/event.html`, `wff/web/static/{app.css,app.js}`, `tests/{test_autotune,test_web}.py`, `.claude/launch.json` |
| C-20260812-2231-home-numbers-first | 2026-08-13 00:34 | Home page rebuilt as wireframe C + total time recorded for every run | **Adarsh picked "numbers first" from five wireframes; built in its indigo palette** (`#4B3F9E` accent, `#ECEBF3` band), verified in-browser against the real store. Health of the project reads first: 46px numbers for photos processed / faces found / speed / machine time / people, then the start card lifted over the band, then guidance, then history. Type scale raised throughout (body 15→16px, section headings 16→21px) — his two complaints were small fonts and no section distinction. **The headline feature: `wff/runs.py`, a per-event `runs.jsonl`.** Timing used to be computed during a run, printed, and lost on exit, so "how long did Manali take?" had no answer the next morning. Now wall-clock total AND Stage-1-only time are journalled on **every** ending — done, failed and stopped — from the console *and* the CLI (`--reviewer`), because if only the console recorded, terminal runs would be missing from every total. The six pre-existing folders honestly read **"not recorded"** rather than being back-filled with a guess, which would have poisoned the average the forecast is built on. Identity is asked once on first open (Adarsh/Devesh/other), remembered, stamped on runs and judgements. Guidance is **derived, not written**: it reads the real distribution (642 of 1,202 face-bearing photos hold exactly one face, max 15 anywhere) and each tip disappears once dealt with — the "open 6 folders to count people" nudge vanished the moment the backfill ran. Also: "same folder twice" detection via `source_folder_id`, so 1,765 processed is reported as **914 distinct** rather than flattering itself; and `too_small_to_judge` so Rishikesh-mini reads "too small to tell" instead of "0 people". **Four real bugs found by running it, all fixed**: (a) `_record_run` read `job.photos_processed`, which does not exist — every run silently failed to record while reporting success; (b) a job read as *finished* before its timing was written, so a reload on that boundary showed "not recorded" for a timed run and killing the server there lost it permanently — `finished` now requires `recorded`; (c) a run failing in 3 ms rounded to `0.0s` and rendered as "not recorded", indistinguishable from never-measured — floored to 0.01s and read as "under 1s"; (d) `/api/forecast` rebuilt the whole dashboard per keystroke (~600 ms), so the number on screen lagged a character behind — now reads only the run journals, **600 ms → 1 ms**. That last one exposed `list_keys` walking ~10,000 crops to learn 6 event names, fixed with a new `ObjectStore.list_prefixes` (delimiter listing, exactly what S3/R2 do natively) — home page 2.86s → 1.15s. 26 new tests, **130 total, all green**, including a reflection test that would have caught bug (a) and a photo-weighted-throughput test proving a 3-photo run cannot swing the forecast. **Nothing committed.** · `wff/runs.py`, `wff/web/{app,data,jobs}.py`, `wff/web/templates/{base,index}.html`, `wff/web/static/{app.css,app.js}`, `wff/cli.py`, `wff/storage/{base,local}.py`, `tests/{test_runs,test_storage}.py` |
| C-20260812-2345-review-wireframes | 2026-08-13 00:10 | Wireframes for the review page — design only | Five layouts of the review page in the chosen "numbers first" palette (A one-page-three-questions · B faces-first-with-docked-controls · C four tabs · D collapsed sections · E contents rail), plus three designs for the tuning controls and two for the progress screen, a comparison table and a recommendation. Also wrote the plain-language explanation of the thresholds that existed nowhere: fingerprints → a 0.00–1.00 number, each knob renamed in real words with its guest-facing consequence, and an SVG plotting the measured `ev_manali01` bands (same person 0.29–0.45, different people ≥0.61) with 0.42/0.55 sitting in the gap. **No product code touched — Adarsh asked explicitly for wireframes only.** Implementation, when it starts, overlaps `wff/web/templates/base.html`, `static/app.css`, `static/app.js`, `wff/web/app.py`, all held by C-20260812-2231-home-numbers-first. · `.claude/WORKLOG.md` |
| C-20260812-2013-testing-ui | 2026-08-12 21:04 | Local web testing platform for Adarsh + Devesh | **Built and verified end to end on real data.** `python -m wff ui` → a light-theme local page: paste a folder, watch progress with a working Stop, then review **every pile in full** (Person 1 renders all 241 crops, vs the old report's 60-face cap). Threshold knobs are query params so a tuning run is a shareable URL and re-groups in **0.26s** against the cached parquet (first load 5.25s). Click any crop → the original photo with every detected face boxed, positioned by percentage of the original so it survives any preview-size change. **The design decision that matters: no judgement is ever stored against a `person_id`** — person ids are reshuffled by every threshold change, so approvals/flags are stored as claims about *face ids* (`same_group` / `different_pairs`), union-found back into partial ground truth. That makes real-wedding precision/recall measurable for the first time (previously only the cricket folder could be scored) and gives an evidence-based threshold suggestion. Plus a judge mode: two crops, S/D keys, pairs chosen from the merge boundary where an answer actually moves the setting. Reused Stage 1/Stage 2 unchanged — one 3-line `should_stop` hook in `stage1.py` is the only edit outside `wff/web/`. **Four real bugs found by using it, all fixed**: `window.WFF` defined after `app.js` loaded (every request went to `/api/events/undefined/`); unknown event id returned an HTML 500 the browser then failed to parse as JSON; flagging one face inside an approved pile silently destroyed the whole approval (241 judged faces → 0); and **pair counts scale quadratically**, so one misplaced face read as "240 wrongly merged" and that same lopsidedness drove the suggested threshold to **1.00 — a setting that merges every guest into one person**. Scoring is now reported in faces, and the threshold sweep compares rates. 50 new tests, 104 total, all green. **Nothing committed.** · `wff/web/**`, `wff/cli.py`, `wff/stage1.py`, `requirements.txt`, `tests/test_web.py`, `tests/test_feedback.py`, `.claude/launch.json` |
| C-20260812-1613-manali-run | 2026-08-12 17:35 | Run Stage 1 + Stage 2 on a real 851-photo folder | **First run on real photos, zero failures.** 7.0 GB Google Takeout — 641 iPhone `.HEIC` + 178 JPG decoded at 12.2 MP with EXIF timestamps, 231 videos and 1,254 JSON sidecars correctly skipped. 1,806 faces detected, 1,182 accepted, 2.27 s/photo, → **20 people** (159 tight groups → 66 evidence-based merges), 155 leftovers kept. Report eyeballed: Person 1 is 241 faces across 241 photos, visibly one man across sunglasses/lighting/angle changes; other large clusters equally clean. The same-photo rule fired once on real data. **Measured for the first time**: the real face-size distribution (28.9% of detections below the 112px ArcFace needs), the clustering safety margin (closest different people 0.611 vs `pass2_threshold` 0.55 — so ~0.61 is the empirical ceiling), and the tiling A/B (**+3.9% accepted faces for 2.34× CPU**, with sub-112px faces up 85→126). Caveat recorded prominently: this is a selfie-heavy trip folder, not a wedding — the back-row-of-a-group case tiling exists for is barely present, so the +3.9% is a floor, not a verdict. Two NMS weaknesses surfaced (one fixed, one flagged). · `.claude/WORKLOG.md` |
| C-20260812-1705-nms-containment | 2026-08-12 17:20 | Duplicate detection slipped past NMS on a real photo | Found because Person 7 clustered as **48 faces in 47 photos** — one person twice in one photo, which cannot happen. Cause (IMG_3526.HEIC): whole-image pass found a face at 286×389, a tile found a degenerate 116×382 **sliver of the same face**, IoU **0.398** vs a 0.40 threshold, so both survived and both were embedded. IoU under-penalises a small box inside a large one because the union is inflated. Fixed by also suppressing on **containment** (intersection ÷ smaller area > 0.80), plus a degenerate-shape gate whose bounds were chosen by measuring 1,806 real boxes (median w/h 0.787, p1 of accepted 0.627, only 2 below 0.35 — both slivers). Verified on the offending photo: 5 detections → 4, duplicate gone, 2 accepted faces = 2 genuinely different people. 2 regression tests added, including one asserting a small nearby face (child in front of an adult) is NOT suppressed. 54 tests green. · `wff/process/detect.py`, `wff/process/quality.py`, `wff/config.py`, `tests/test_detect_geometry.py` |
| C-20260812-1612-dataset-retry | 2026-08-12 16:30 | Dataset script: one throttled API call killed the whole multi-player run | Found the actual cause of the earlier failures: `download()` had 429 retry-backoff but **`api_get()` had none**, so a throttled API call raised `HTTPError: 429` straight out of the loop and destroyed every player after it — which is why a 12-player run died at player 5. `api_get` now retries with exponential backoff and honours the `Retry-After` header, and `main()` catches per-player failures so one player cannot cost the rest. **Verified live**: the run hit a 429, printed `API throttled (429), waiting 36s...`, recovered, and completed ("Wrote 7 images across 3 players") instead of crashing. Commons still throttles this IP so the yield stays low, but the script is now correct and unattended-safe. Largely moot for validation either way — Adarsh supplied a real 851-photo folder, which supersedes the cricket set. · `scripts/build_test_dataset.py` |
| C-20260812-1436-claude-md | 2026-08-12 14:40 | Create project `CLAUDE.md` | Adarsh asked, in chat, that every agent be told he is building software for the first time and that jargon-heavy explanations and questions are wasting both our time. Written as **rule #1** at the top of a new project `CLAUDE.md`, with worked examples of a bad vs. good question (ask about the product consequence, not the plumbing; if a decision has no consequence he could feel, decide it silently). Rest of the file is short context an agent needs on day one: the one-paragraph product, what exists today (CLI only, `python -m wff run <id> <folder>` → `report.html`), the four standing code rules (stage split, no filesystem paths downstream of ingest, never commit, DPDP), and the real bottleneck. No code touched. · `CLAUDE.md` |
| C-20260812-1440-dataset-thumbs | 2026-08-12 15:05 | Fix Wikimedia 429 throttling in the test-dataset script | Commons rate-limits full-size downloads hard enough that a 140-image run yielded 18 images (1–2 per person). Patched `fetch_image_info` to request a rendered thumbnail (`iiurlwidth=1600`) and download that instead of the original — which is exactly what Commons' own 429 response tells you to do. Spot check: 5/5 images kept per player vs ~1/14 before. Falls back to the original when Commons returns no thumbnail (originals under 1600px come back "unscaled"). 1600px still leaves faces far above the 112px ArcFace needs, and per `docs/ARCHITECTURE.md` these cropped press photos could never test the resolution work regardless. **A full rebuild still failed** — a pre-patch background run that could not be terminated kept consuming the shared rate budget. Retry once that process exits. · `scripts/build_test_dataset.py` |
| C-20260812-1351-stage1-stage2 | 2026-08-12 15:05 | Initial development: Stage 1 + Stage 2, local scripts only | Built the whole week-1–2 slice and **ran it end to end on real photos**: `wff run ev_cricket01 test-data/cricket-players` → 17 photos, 29 faces detected, 23 accepted, 0.69 s/photo CPU-only, `faces.parquet` + previews + thumbs + aligned crops, clustered, HTML report rendered and verified in-browser. **precision 1.000, 0 contaminated clusters** (recall 0.075 is expected and meaningless — the dataset has 1–2 faces for 8 of 9 people). Package: `wff/{config,storage,ingest,process,cluster,stage1,stage2,report,cli}`. Chose NOT to depend on `insightface` (no py3.12 Windows wheel; its wrapper hides per-tile detection; keeps landmine #1 a one-file swap) — SCRFD anchor decoding and Umeyama alignment implemented directly on ONNX Runtime. The no-local-paths rule is **enforced** by `parse_uri()` raising on scheme-less strings, with a test. **Measured the central architecture claim for the first time**: tiling gains nothing at 200px faces, +3/12 at 90px, and **12 → 0 vs 11 found at 60px** — without tiling the whole-image pass finds nothing at that size. Found and fixed two real defects: pyarrow cannot read back a null inside a `fixed_size_list` (killed consolidation), and exact agglomerative clustering is O(n²) memory so it would have **OOM'd at wedding #1** with 32k faces — now k-means-blocked above 12k, which can only split (safe) and pass 2 recovers. 52 tests, all passing, aimed at what fails silently. **Nothing committed — Adarsh reviews and pushes.** Thresholds are untuned guesses; real photos remain the blocker. · `wff/**`, `tests/**`, `requirements.txt`, `.gitignore` |
| C-20260811-1850-arch-md | 2026-08-11 19:04 | Write the full 8-step architecture as a markdown doc | Created `docs/ARCHITECTURE.md` (Draft 2), superseding `docs/architecture.html` (kept for history, not deleted). Covers: what changed from Draft 1 and why; the three locked changes (**link-first ingest**, **native-resolution processing**, **download policy A+B**); a plain-language pixel primer explaining why shrinking-before-detecting produces confident *wrong* matches; the full 8-step pipeline (Ingest → Manifest → Process → Cluster → Publish → Review → Gallery → Search) with tech, edge cases and a free-now/paid-later column at every step; the ingest adapter pattern across Drive/Dropbox/zip/OneDrive/upload; tiled detection, alignment and EXIF rotation; two-pass clustering with the same-photo rule; the complete SQL data model including the `person_photos` materialised join and the Postgres/parquet storage split; two-tier selfie search; the ₹0 free stack incl. the Cloudflare Tunnel trick; landmines (Drive one **defused**, InsightFace licence + DPDP still open); build order; 6 open items. **Nothing committed — Adarsh reviews and pushes.** No product code written; still zero implementation. · `docs/ARCHITECTURE.md`, `.claude/WORKLOG.md` |
| C-20260810-2033-answers-dataset | 2026-08-10 20:55 | Record Devesh's answers to the 5 open questions; build a cricket-player test dataset script | Added a "Devesh's answer" callout under Q1–Q4 in `docs/architecture.html` (Q5 left open, not yet answered). Wrote `scripts/build_test_dataset.py` (stdlib-only) — resolves each player to a Wikimedia Commons category, filters out logos/flags/signatures, downloads freely-licensed photos plus a `manifest.csv` of source URL + license per image, with 429 retry-backoff. Test run on 2 players confirmed real JPEGs + correct license metadata download end to end. Added `.gitignore` so `test-data/` output never gets committed. · `docs/architecture.html`, `scripts/build_test_dataset.py`, `.gitignore` |
| C-20260810-1830-arch-doc | 2026-08-10 18:36 | Single-page architecture review doc for Adarsh + Devesh | Built `docs/architecture.html` — self-contained single file, zero JS, zero external assets, light/dark aware, GitHub-Pages-ready. 12 numbered sections: product + both user journeys, the never-store-originals rule with the 50 GB→1.1 GB table, five-box pipeline diagram, box-by-box tech choices each with alternatives + verdict, 6-table data model, step-by-step trace of one photo and one guest search, cost table in ₹ with the 90-day retention policy, the three landmines (InsightFace non-commercial weights / Drive restricted scope / DPDP) + the Vercel one, decisions locked 10 Aug incl. the stage-1-cached / stage-2-cheap split, build order, who-does-what, and 5 numbered open questions for Devesh to reply to. Verified in-browser: 12/12 TOC anchors resolve, 0 broken links, 0 horizontal page overflow at 375px and desktop, wide tables scroll inside their own containers, pipeline diagram stacks on mobile. **Nothing committed — Adarsh reviews and pushes.** No product code written; repo still has zero commits. |
